from nat import Nat
numerals Nat

/// Hard problem: a small decimal arithmetic bridge from the miniF2F project
/// over natural-number literals.
theorem nat_decimal_arithmetic_bridge {
    Nat.23 * Nat.3 + Nat.2 = Nat.71
}
