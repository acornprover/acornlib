// The decimal-arithmetic bridge goal `Nat.23 * Nat.3 + Nat.2 = Nat.71` that
// used to live here is now solved by the nat package: the normalization lemmas
// and the concrete product/addition table live in `nat.nat_decimal_bridge`
// (exported through the `nat` interface), and the goal itself is proved as
// `nat_mul_23_3_add_2` and used by miniF2F problem `mathd_numbertheory_81`
// (`src/minif2f/valid/numbertheory.ac`).
