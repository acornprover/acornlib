{"goal":"g.pow(q).congr_mod(1, p)","proof":["function(x0: Nat, x1: Nat, x2: Nat) { lib(nat.nat_base).nat_mod(x0, x1) = lib(nat.nat_base).nat_mod(x2, x1) = x0.congr_mod(x2, x1) }(g.pow(q), p, 1)","function(x0: Nat, x1: Nat) { lib(nat.nat_base).nat_mod(lib(nat.nat_base).nat_mod(x0, x1), x1) = lib(nat.nat_base).nat_mod(x0, x1) }(g.pow(q), p)"]}
{"goal":"1.pow(k) = 1","proof":["function(x0: Nat) { 1.pow(x0) = 1 }(k)"]}
{"goal":"g.pow(q).pow(k).congr_mod(1, p)","proof":[]}
{"goal":"g.pow(r).congr_mod(g.pow(r), p)","proof":[]}
{"goal":"(g.pow(q).pow(k) * g.pow(r)).congr_mod(1 * g.pow(r), p)","proof":[]}
{"goal":"lib(nat.nat_base).nat_mod(g.pow(q).pow(k) * g.pow(r))(p) = lib(nat.nat_base).nat_mod(1 * g.pow(r))(p)","proof":["function(x0: Nat, x1: Nat, x2: Nat) { lib(nat.nat_base).nat_mod(x0, x1) = lib(nat.nat_base).nat_mod(x2, x1) = x0.congr_mod(x2, x1) }(g.pow(q).pow(k) * g.pow(r), p, 1 * g.pow(r))"]}
{"goal":"1 * g.pow(r) = g.pow(r)","proof":["function(x0: Nat) { 1 * x0 = x0 }(g.pow(r))"]}
{"goal":"lib(nat.nat_base).nat_mod(1 * g.pow(r))(p) = lib(nat.nat_base).nat_mod(g.pow(r))(p)","proof":[]}
{"goal":"lib(nat.nat_base).nat_mod(g.pow(q).pow(k) * g.pow(r))(p) = lib(nat.nat_base).nat_mod(g.pow(r))(p)","proof":[]}
{"goal":"k * q = q * k","proof":["function(x0: Nat, x1: Nat) { x0 * x1 = x1 * x0 }(k, q)"]}
{"goal":"g.pow(k * q) = g.pow(q).pow(k)","proof":["function(x0: Nat, x1: Nat, x2: Nat) { x0.pow(x1).pow(x2) = x0.pow(x1 * x2) }(g, q, k)"]}
{"goal":"g.pow(k * q + r) = g.pow(k * q) * g.pow(r)","proof":["function(x0: Nat, x1: Nat, x2: Nat) { x0.pow(x1) * x0.pow(x2) = x0.pow(x1 + x2) }(g, k * q, r)"]}
{"goal":"lib(nat.nat_base).nat_mod(g.pow(k * q + r))(p) = lib(nat.nat_base).nat_mod(g.pow(r))(p)","proof":[]}
{"goal":"dsa_pow_order_reduce","proof":[]}
{"goal":"exists(k0: Nat) { k0 * q + lib(nat.nat_base).nat_mod(a)(q) = a }","proof":[]}
{"goal":"lib(nat.nat_base).nat_mod(g.pow(k * q + lib(nat.nat_base).nat_mod(a)(q)))(p) = lib(nat.nat_base).nat_mod(g.pow(lib(nat.nat_base).nat_mod(a)(q)))(p)","proof":[]}
{"goal":"lib(nat.nat_base).nat_mod(g.pow(a))(p) = lib(nat.nat_base).nat_mod(g.pow(lib(nat.nat_base).nat_mod(a)(q)))(p)","proof":[]}
{"goal":"dsa_pow_mod_q","proof":[]}
{"goal":"lib(nat.nat_base).nat_mod(g.pow(x))(p).congr_mod(g.pow(x), p)","proof":[]}
{"goal":"y.congr_mod(g.pow(x), p)","proof":[]}
{"goal":"y.pow(b).congr_mod(g.pow(x).pow(b), p)","proof":[]}
{"goal":"g.pow(x * b) = g.pow(x).pow(b)","proof":["function(x0: Nat, x1: Nat, x2: Nat) { x0.pow(x1).pow(x2) = x0.pow(x1 * x2) }(g, x, b)"]}
{"goal":"y.pow(b).congr_mod(g.pow(x * b), p)","proof":[]}
{"goal":"dsa_y_pow_congr","proof":[]}
{"goal":"y.pow(b).congr_mod(g.pow(x * b), p)","proof":[]}
{"goal":"g.pow(a).congr_mod(g.pow(a), p)","proof":[]}
{"goal":"(g.pow(a) * y.pow(b)).congr_mod(g.pow(a) * g.pow(x * b), p)","proof":[]}
{"goal":"g.pow(a) * g.pow(x * b) = g.pow(a + x * b)","proof":["function(x0: Nat, x1: Nat, x2: Nat) { x0.pow(x1) * x0.pow(x2) = x0.pow(x1 + x2) }(g, a, x * b)"]}
{"goal":"lib(nat.nat_base).nat_mod(g.pow(a) * y.pow(b))(p) = lib(nat.nat_base).nat_mod(g.pow(a + x * b))(p)","proof":["function(x0: Nat, x1: Nat, x2: Nat) { lib(nat.nat_base).nat_mod(x0, x1) = lib(nat.nat_base).nat_mod(x2, x1) = x0.congr_mod(x2, x1) }(g.pow(a) * y.pow(b), p, g.pow(a) * g.pow(x * b))"]}
{"goal":"dsa_combine_pow","proof":[]}
{"goal":"lib(nat.nat_base).nat_mod(h * sinv)(q).congr_mod(h * sinv, q)","proof":[]}
{"goal":"lib(nat.nat_base).nat_mod(r * sinv)(q).congr_mod(r * sinv, q)","proof":[]}
{"goal":"x.congr_mod(x, q)","proof":[]}
{"goal":"(x * lib(nat.nat_base).nat_mod(r * sinv)(q)).congr_mod(x * (r * sinv), q)","proof":[]}
{"goal":"(lib(nat.nat_base).nat_mod(h * sinv)(q) + x * lib(nat.nat_base).nat_mod(r * sinv)(q)).congr_mod(h * sinv + x * (r * sinv), q)","proof":[]}
{"goal":"h * sinv + x * (r * sinv) = (h + x * r) * sinv","proof":["function(x0: Nat, x1: Nat, x2: Nat) { x0 * x1 + x2 * x1 = (x0 + x2) * x1 }(h, sinv, x * r)","function(x0: Nat, x1: Nat, x2: Nat) { x0 * x1 * x2 = x0 * (x1 * x2) }(x, r, sinv)"]}
{"goal":"lib(nat.nat_base).nat_mod(lib(nat.nat_base).nat_mod(h * sinv)(q) + x * lib(nat.nat_base).nat_mod(r * sinv)(q))(q) = lib(nat.nat_base).nat_mod((h + x * r) * sinv)(q)","proof":["function(x0: Nat, x1: Nat, x2: Nat) { lib(nat.nat_base).nat_mod(x0, x1) = lib(nat.nat_base).nat_mod(x2, x1) = x0.congr_mod(x2, x1) }(lib(nat.nat_base).nat_mod(h * sinv, q) + x * lib(nat.nat_base).nat_mod(r * sinv, q), q, h * sinv + x * (r * sinv))"]}
{"goal":"dsa_exp_mod_q_factor","proof":[]}
{"goal":"k * (kinv * (h + x * r)) = k * kinv * (h + x * r)","proof":["function(x0: Nat, x1: Nat, x2: Nat) { x0 * x1 * x2 = x0 * (x1 * x2) }(k, kinv, h + x * r)"]}
{"goal":"(k * s).congr_mod(k * kinv * (h + x * r), q)","proof":[]}
{"goal":"1 * (h + x * r) = h + x * r","proof":["function(x0: Nat) { 1 * x0 = x0 }(h + x * r)"]}
{"goal":"(k * kinv * (h + x * r)).congr_mod(h + x * r, q)","proof":[]}
{"goal":"(k * s).congr_mod(h + x * r, q)","proof":[]}
{"goal":"dsa_k_times_s_congr","proof":[]}
{"goal":"((h + x * r) * sinv).congr_mod(k * s * sinv, q)","proof":[]}
{"goal":"k * s * sinv = k * (s * sinv)","proof":["function(x0: Nat, x1: Nat, x2: Nat) { x0 * x1 * x2 = x0 * (x1 * x2) }(k, s, sinv)"]}
{"goal":"((h + x * r) * sinv).congr_mod(k * (s * sinv), q)","proof":[]}
{"goal":"(k * (s * sinv)).congr_mod(k * 1, q)","proof":[]}
{"goal":"k * 1 = k","proof":["function(x0: Nat, x1: Nat) { x0 * x1 = x1 * x0 }(1, k)","function(x0: Nat) { 1 * x0 = x0 }(k)"]}
{"goal":"(k * (s * sinv)).congr_mod(k, q)","proof":[]}
{"goal":"((h + x * r) * sinv).congr_mod(k, q)","proof":[]}
{"goal":"dsa_finish_congr","proof":[]}
{"goal":"lib(nat.nat_base).nat_mod(lhs)(q) = lib(nat.nat_base).nat_mod(k)(q)","proof":["function(x0: Nat, x1: Nat, x2: Nat) { lib(nat.nat_base).nat_mod(x0, x1) = lib(nat.nat_base).nat_mod(x2, x1) = x0.congr_mod(x2, x1) }(lhs, q, k)"]}
{"goal":"lib(nat.nat_base).nat_mod(k)(q) = k","proof":[]}
{"goal":"lib(nat.nat_base).nat_mod(lhs)(q) = k","proof":[]}
{"goal":"dsa_congr_to_eq","proof":[]}
{"goal":"lib(nat.nat_base).nat_mod(1)(q) = 1","proof":[]}
{"goal":"s.congr_mod(kinv * (h + x * r), q)","proof":[]}
{"goal":"lib(nat.nat_base).nat_mod(k * kinv)(q) = lib(nat.nat_base).nat_mod(1)(q)","proof":[]}
{"goal":"(k * kinv).congr_mod(1, q)","proof":["function(x0: Nat, x1: Nat, x2: Nat) { lib(nat.nat_base).nat_mod(x0, x1) = lib(nat.nat_base).nat_mod(x2, x1) = x0.congr_mod(x2, x1) }(k * kinv, q, 1)"]}
{"goal":"(k * s).congr_mod(h + x * r, q)","proof":[]}
{"goal":"dsa_k_times_s_from_mod","proof":[]}
