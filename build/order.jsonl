{"goal":"a.max(b) >= b","proof":["b <= b or b <= b","a.max(b) >= b = b <= a.max(b)"]}
{"goal":"a.max(b) >= b","proof":["a.max(b) = b or b <= a","not b <= a or a.max(b) = a","a >= b = b <= a"]}
{"goal":"max_imp_gte","proof":["not is_transitive<L>(L.lte) or not b <= a.max(b) or not a.max(b) <= a or b <= a","not b <= a or a.max(b) = a","a.max(b) <= a or a <= a.max(b)","a.max(b) >= a = a <= a.max(b)","a.max(b) >= b = b <= a.max(b)"]}
{"goal":"a.min(b) <= b","proof":["a.min(b) = b or a <= b","b <= b"]}
{"goal":"a.min(b) <= b","proof":["a.min(b) = b or a <= b","not a <= b or a.min(b) = a","b <= b"]}
{"goal":"min_imp_lte","proof":["not is_transitive<L>(L.lte) or not a <= a.min(b) or not a.min(b) <= b or a <= b","not a <= b or a.min(b) = a","a <= a.min(b) or a.min(b) <= a"]}
{"goal":"lt_imp_min","proof":["not a <= b or a.min(b) = a","not a < b or a <= b"]}
{"goal":"not_lt_imp_min","proof":["not a <= b or a < b or b = a","a <= b or a.min(b) = b","not a <= b or a.min(b) = a"]}
{"goal":"gt_imp_min","proof":["not is_antisymmetric<L>(L.lte) or not b <= a or not a <= b or b = a","a.min(b) = b or a <= b","not a <= b or a.min(b) = a","b < a = a > b","not b < a or b <= a"]}
{"goal":"not_gt_imp_min","proof":["not b <= a or b < a or b = a","not a <= b or a.min(b) = a","not a < b or a.min(b) = a","a > b = b < a","b <= a or a <= b"]}
{"goal":"gte_imp_min","proof":["not b <= a or b < a or b = a","not a > b or a.min(b) = b","a.min(b) = b or a < b","b <= a = a >= b","a > b = b < a"]}
{"goal":"not_gte_imp_min","proof":["not a <= b or a.min(b) = a","a >= b = b <= a","a <= b or b <= a"]}
{"goal":"lt_imp_max","proof":["not is_antisymmetric<L>(L.lte) or not a <= b or not b <= a or b = a","a.max(b) = b or b <= a","not a < b or a <= b","not a < b or b != a"]}
{"goal":"not_lt_imp_max","proof":["not a <= b or a < b or b = a","not b <= a or a.max(b) = a","a <= b or b <= a"]}
{"goal":"gt_imp_max","proof":["not b <= a or a.max(b) = a","b < a = a > b","not b < a or b <= a"]}
{"goal":"not_gt_imp_max","proof":["not b <= a or b < a or b = a","b <= a or a.max(b) = b","not b <= a or a.max(b) = a","b < a = a > b"]}
{"goal":"gte_imp_max","proof":["not b <= a or a.max(b) = a","b <= a = a >= b"]}
{"goal":"not_gte_imp_max","proof":["b <= a or a.max(b) = b","b <= a = a >= b"]}
{"goal":"min_is_one","proof":["a <= b or a.min(b) = b","not a <= b or a.min(b) = a"]}
{"goal":"max_is_one","proof":["b <= a or a.max(b) = b","not b <= a or a.max(b) = a"]}
{"goal":"b.min(a) = a.min(b)","proof":["b >= a or b.min(a) = b","a.min(b) = b or a <= b","b >= a = a <= b"]}
{"goal":"min_symm","proof":["not b >= a or b.min(a) = a","not a <= b or a.min(b) = a","b >= a = a <= b"]}
{"goal":"b.max(a) = a.max(b)","proof":["a <= b or b.max(a) = a","not b <= a or a.max(b) = a","b <= a or a <= b"]}
{"goal":"max_symm","proof":["not a <= b or a < b or b = a","not a < b or a.max(b) = b","not a <= b or b.max(a) = b"]}
