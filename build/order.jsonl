{"goal":"a.max(b) >= b","proof":["b <= b or b <= b","a.max(b) >= b = b <= a.max(b)","not b <= a.max(b)","not b <= b"]}
{"goal":"a.max(b) >= b","proof":["a.max(b) = b or b <= a","b <= a","not b <= a or a.max(b) = a","a >= b = b <= a","a >= b","a.max(b) = a"]}
{"goal":"max_imp_gte","proof":["not a.max(b) >= a","not is_transitive[L](L.lte) or not b <= a.max(b) or not a.max(b) <= a or b <= a","not b <= a or a.max(b) = a","a <= a or a <= a","a.max(b) <= a or a <= a.max(b)","a.max(b) >= a = a <= a.max(b)","a.max(b) >= b = b <= a.max(b)","not a <= a.max(b)","is_transitive[L](L.lte)","b <= a.max(b)","a <= a","a.max(b) <= a","not a.max(b) <= a or b <= a","b <= a","a.max(b) = a"]}
{"goal":"a.min(b) <= b","proof":["not a <= b","a.min(b) = b or a <= b","b <= a or a <= b","b <= a","a >= b = b <= a","b >= a.min(b) = a.min(b) <= b","a.min(b) = b"]}
{"goal":"a.min(b) <= b","proof":["a.min(b) = b or a <= b","not a <= b or a.min(b) = a","not a <= b","b <= b or b <= b","b <= b","a.min(b) = b"]}
{"goal":"min_imp_lte","proof":["not a.min(b) <= a","not is_transitive[L](L.lte) or not a <= a.min(b) or not a.min(b) <= b or a <= b","not a <= b or a.min(b) = a","a <= a or a <= a","a <= a.min(b) or a.min(b) <= a","is_transitive[L](L.lte)","a <= a","a <= a.min(b)","not a <= a.min(b) or not is_transitive[L](L.lte) or a <= b","a <= b","a.min(b) = a"]}
{"goal":"lt_imp_min","proof":["not a <= b or a.min(b) = a","not a <= b","not a < b or a <= b"]}
{"goal":"not_lt_imp_min","proof":["not a <= b or a < b or b = a","a.min(b) = b or a <= b","a <= b","b = a","not a <= b or a.min(b) = a","a.min(b) != a","a.min(b) = a"]}
{"goal":"gt_imp_min","proof":["not is_antisymmetric[L](L.lte) or not b <= a or not a <= b or b = a","a.min(b) = b or a <= b","a <= b","a > b = b < a","b < a","not b < a or b <= a","not b < a or b != a","is_antisymmetric[L](L.lte)","b != a","b <= a"]}
{"goal":"not_gt_imp_min","proof":["b.min(a) = a or b < a","not a <= b or a.min(b) = a","not a <= b","a > b = b < a","not b < a","b.min(a) <= b","b.min(a) = a"]}
{"goal":"gte_imp_min","proof":["not b <= a or b < a or b = a","not a > b or a.min(b) = b","not a > b","a.min(b) = b or a < b","a < b","a > b = b < a","not b < a","a >= b = b <= a","b <= a","not a < b or b != a","b = a","b != a"]}
{"goal":"not_gte_imp_min","proof":["not a <= b or a.min(b) = a","not a <= b","a <= b or b <= a","a >= b = b <= a","b <= a","not b <= a"]}
{"goal":"lt_imp_max","proof":["not is_antisymmetric[L](L.lte) or not a <= b or not b <= a or b = a","a.max(b) = b or b <= a","b <= a","not a < b or a <= b","not a < b or b != a","b != a","is_antisymmetric[L](L.lte)","a <= b"]}
{"goal":"not_lt_imp_max","proof":["a.min(b) = b or a < b","not b <= a or a.max(b) = a","not b <= a","a.min(b) <= a","a.min(b) = b"]}
{"goal":"gt_imp_max","proof":["not b <= a or a.max(b) = a","not b <= a","a > b = b < a","b < a","not b < a or b <= a"]}
{"goal":"not_gt_imp_max","proof":["not b <= a or b < a or b = a","a.max(b) = a or a < b","not a < b or a.max(b) = b","not a < b","a.max(b) = b or b <= a","b <= a","a > b = b < a","not b < a","b = a","a.max(b) = a","a.max(b) != a"]}
{"goal":"gte_imp_max","proof":["not b <= a or a.max(b) = a","not b <= a","a >= b = b <= a"]}
{"goal":"not_gte_imp_max","proof":["a.max(b) = b or b <= a","b <= a","a >= b = b <= a"]}
{"goal":"min_is_one","proof":["a.min(b) = a or a >= b","a >= b","not a >= b or a.min(b) = b"]}
{"goal":"max_is_one","proof":["a.max(b) = b or a >= b","a >= b","not a >= b or a.max(b) = a"]}
{"goal":"b.min(a) = a.min(b)","proof":["b.min(a) = b or b >= a","a.min(b) = b or a <= b","b >= a = a <= b","not b >= a","a.min(b) = b","b.min(a) = b","b.min(a) != b"]}
{"goal":"min_symm","proof":["not b >= a or b.min(a) = a","not a <= b or a.min(b) = a","a.min(b) = a","b >= a = a <= b","b >= a","b.min(a) != a","b.min(a) = a"]}
{"goal":"b.max(a) = a.max(b)","proof":["a.max(b) = a or a < b","b.max(a) = a or a <= b","not a < b or a <= b","not a < b","b.max(a) = a","a.max(b) = a","a.max(b) != a"]}
{"goal":"max_symm","proof":["not is_antisymmetric[L](L.lte) or not a <= b or not b <= a or b = a","b.max(a).max(b.max(a)) = b.max(a) or b.max(a).max(b.max(a)) = b.max(a)","a.max(b) = b or b <= a","not a <= b or b.max(a) = b","b.max(a) = b","is_antisymmetric[L](L.lte)","a.max(b) != b","b.max(a).max(b.max(a)) = b.max(a)","not is_antisymmetric[L](L.lte) or not b <= a or b = a","b.max(a).max(b.max(a)) != a.max(b)","b.max(a).max(b) != a.max(b)","b.max(a) != a","b != a","b <= a","not b <= a"]}
