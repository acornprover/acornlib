{"goal":"p(List.nil[T])","proof":["c ∈ List.nil[T] or p(List.nil[T])","c ∈ List.nil[T]","not c ∈ List.nil[T] or List.nil[T] != List.nil[T]"]}
{"goal":"head <= ub","proof":["not is_upper_bound(List.cons(head, tail), ub) or List.cons(head, tail) != List.cons(head, tail) or head <= ub","not is_upper_bound(List.cons(head, tail), ub) or List.cons(head, tail) != List.cons(head, tail)","not is_upper_bound(List.cons(head, tail), ub)"]}
{"goal":"is_upper_bound(tail, ub)","proof":["not is_upper_bound(List.cons(head, tail), ub) or List.cons(head, tail) != List.cons(head, tail) or is_upper_bound(tail, ub)","not is_upper_bound(List.cons(head, tail), ub) or List.cons(head, tail) != List.cons(head, tail)","not is_upper_bound(List.cons(head, tail), ub)"]}
{"goal":"c <= ub","proof":[]}
{"goal":"tail.contains(c)","proof":["not c ∈ List.cons(head, tail) or List.cons(head, tail) != List.cons(head, tail) or c ∈ tail or head = c","not c ∈ List.cons(head, tail) or List.cons(head, tail) != List.cons(head, tail) or head = c","List.cons(head, tail) != List.cons(head, tail) or not c ∈ List.cons(head, tail)"]}
{"goal":"c <= ub","proof":["not is_upper_bound(tail, ub) or not c ∈ tail or not p(tail) or c <= ub","not is_upper_bound(tail, ub) or not c ∈ tail or not p(tail)","not p(tail)"]}
{"goal":"c <= ub","proof":[]}
{"goal":"p(List.cons(head, tail))","proof":["not c <= ub or p(List.cons(head, tail))","not c <= ub","c ∈ List.cons(head, tail) or p(List.cons(head, tail))","is_upper_bound(List.cons(head, tail), ub) or p(List.cons(head, tail))","is_upper_bound(List.cons(head, tail), ub)","c ∈ List.cons(head, tail)","not c ∈ List.cons(head, tail)"]}
{"goal":"upper_bound_contains","proof":["not is_upper_bound(list, ub) or not c ∈ list or not p(list) or c <= ub","not is_upper_bound(list, ub) or not c ∈ list or not p(list)","let (s0: T, s1: List[T]) satisfy { forall(x0: List[T]) { not p(List.nil[T]) or p(s1) or p(x0) } and forall(x1: List[T]) { not p(List.cons(s0, s1)) or not p(List.nil[T]) or p(x1) } }","not p(List.nil[T]) or p(s1) or p(s1)","p(s1) or p(s1)","not p(List.cons(s0, s1)) or not p(List.nil[T]) or p(list)","not p(List.cons(s0, s1)) or p(list)","not p(s1) or p(List.cons(s0, s1))","p(s1)","not p(list)","not p(List.cons(s0, s1))"]}
{"goal":"p(List.nil[T])","proof":["not is_upper_bound(List.nil[T], ub) or p(List.nil[T])","not is_upper_bound(List.nil[T], ub)","List.nil[T] != List.nil[T] or is_upper_bound(List.nil[T], ub)"]}
{"goal":"List.cons(head, tail) + list2 = List.cons(head, tail + list2)","proof":["List.cons(head, tail) != List.cons(head, tail) or List.cons(head, tail) + list2 = List.cons(head, tail + list2)"]}
{"goal":"is_upper_bound(List.cons(head, tail + list2), ub)","proof":[]}
{"goal":"head <= ub","proof":["not is_upper_bound(List.cons(head, tail) + list2, ub) or List.cons(head, tail) + list2 != List.cons(head, tail + list2) or head <= ub","not is_upper_bound(List.cons(head, tail) + list2, ub) or List.cons(head, tail) + list2 != List.cons(head, tail + list2)"]}
{"goal":"is_upper_bound(tail + list2, ub)","proof":["not is_upper_bound(List.cons(head, tail) + list2, ub) or List.cons(head, tail) + list2 != List.cons(head, tail + list2) or is_upper_bound(tail + list2, ub)","List.cons(head, tail) + list2 != List.cons(head, tail + list2) or not is_upper_bound(List.cons(head, tail) + list2, ub)"]}
{"goal":"is_upper_bound(tail, ub)","proof":["not is_upper_bound(tail + list2, ub) or not p(tail) or is_upper_bound(tail, ub)"]}
{"goal":"is_upper_bound(List.cons(head, tail), ub)","proof":["not is_upper_bound(tail, ub) or not head <= ub or List.cons(head, tail) != List.cons(head, tail) or is_upper_bound(List.cons(head, tail), ub)","not is_upper_bound(tail, ub) or not head <= ub or is_upper_bound(List.cons(head, tail), ub)"]}
{"goal":"p(List.cons(head, tail))","proof":["not is_upper_bound(List.cons(head, tail), ub) or p(List.cons(head, tail))","is_upper_bound(List.cons(head, tail) + list2, ub) or p(List.cons(head, tail))","not is_upper_bound(List.cons(head, tail), ub)","is_upper_bound(List.cons(head, tail) + list2, ub)","not is_upper_bound(List.cons(head, tail) + list2, ub)"]}
{"goal":"upper_bound_add_left","proof":["let (s0: T, s1: List[T]) satisfy { forall(x0: List[T]) { not p(List.nil[T]) or p(s1) or p(x0) } and forall(x1: List[T]) { not p(List.cons(s0, s1)) or not p(List.nil[T]) or p(x1) } }","not p(List.nil[T]) or p(s1) or p(s1)","p(s1) or p(s1)","not p(List.cons(s0, s1)) or not p(List.nil[T]) or p(list1)","not p(List.cons(s0, s1)) or p(list1)","not p(s1) or p(List.cons(s0, s1))","not is_upper_bound(list1 + list2, ub) or not p(list1) or is_upper_bound(list1, ub)","p(s1)","not p(list1)","not p(List.cons(s0, s1))"]}
{"goal":"List.nil[T] + list2 = list2","proof":["List.nil[T] != List.nil[T] or List.nil[T] + list2 = list2"]}
{"goal":"p(List.nil[T])","proof":["not is_upper_bound(list2, ub) or p(List.nil[T])","not is_upper_bound(list2, ub)","is_upper_bound(List.nil[T] + list2, ub) or p(List.nil[T])","not is_upper_bound(List.nil[T] + list2, ub)","is_upper_bound(List.nil[T] + list2, ub)"]}
{"goal":"List.cons(head, tail) + list2 = List.cons(head, tail + list2)","proof":["List.cons(head, tail) != List.cons(head, tail) or List.cons(head, tail) + list2 = List.cons(head, tail + list2)"]}
{"goal":"is_upper_bound(List.cons(head, tail + list2), ub)","proof":[]}
{"goal":"is_upper_bound(tail + list2, ub)","proof":["not is_upper_bound(List.cons(head, tail) + list2, ub) or List.cons(head, tail) + list2 != List.cons(head, tail + list2) or is_upper_bound(tail + list2, ub)","List.cons(head, tail) + list2 != List.cons(head, tail + list2) or not is_upper_bound(List.cons(head, tail) + list2, ub)"]}
{"goal":"is_upper_bound(list2, ub)","proof":["not is_upper_bound(tail + list2, ub) or not p(tail) or is_upper_bound(list2, ub)","not is_upper_bound(tail + list2, ub) or not p(tail)","not is_upper_bound(tail + list2, ub)"]}
{"goal":"p(List.cons(head, tail))","proof":["not is_upper_bound(list2, ub) or p(List.cons(head, tail))","not is_upper_bound(list2, ub)","is_upper_bound(List.cons(head, tail) + list2, ub) or p(List.cons(head, tail))"]}
{"goal":"upper_bound_add_right","proof":["not is_upper_bound(list1 + list2, ub) or not p(list1) or is_upper_bound(list2, ub)","not is_upper_bound(list1 + list2, ub) or not p(list1)","let (s0: T, s1: List[T]) satisfy { forall(x0: List[T]) { not p(List.nil[T]) or p(s1) or p(x0) } and forall(x1: List[T]) { not p(List.cons(s0, s1)) or not p(List.nil[T]) or p(x1) } }","not p(List.nil[T]) or p(s1) or p(s1)","p(s1) or p(s1)","not p(List.cons(s0, s1)) or not p(List.nil[T]) or p(list1)","not p(List.cons(s0, s1)) or p(list1)","not p(s1) or p(List.cons(s0, s1))","p(s1)","not p(list1)","not p(List.cons(s0, s1))"]}
{"goal":"p(List.nil[T])","proof":["not is_upper_bound(List.nil[T], ub2) or p(List.nil[T])","not is_upper_bound(List.nil[T], ub2)","List.nil[T] != List.nil[T] or is_upper_bound(List.nil[T], ub2)"]}
{"goal":"head <= ub1","proof":["not is_upper_bound(List.cons(head, tail), ub1) or List.cons(head, tail) != List.cons(head, tail) or head <= ub1","not is_upper_bound(List.cons(head, tail), ub1) or List.cons(head, tail) != List.cons(head, tail)","not is_upper_bound(List.cons(head, tail), ub1)"]}
{"goal":"is_upper_bound(tail, ub1)","proof":["not is_upper_bound(List.cons(head, tail), ub1) or List.cons(head, tail) != List.cons(head, tail) or is_upper_bound(tail, ub1)","not is_upper_bound(List.cons(head, tail), ub1) or List.cons(head, tail) != List.cons(head, tail)","not is_upper_bound(List.cons(head, tail), ub1)"]}
{"goal":"head <= ub2","proof":["not lib(util).is_transitive[T](T.lte) or not head <= ub1 or not ub1 <= ub2 or head <= ub2","not head <= ub1 or not ub1 <= ub2"]}
{"goal":"is_upper_bound(tail, ub2)","proof":["not ub1 <= ub2 or not is_upper_bound(tail, ub1) or not p(tail) or is_upper_bound(tail, ub2)","not is_upper_bound(tail, ub1) or not p(tail) or is_upper_bound(tail, ub2)"]}
{"goal":"is_upper_bound(List.cons(head, tail), ub2)","proof":["not is_upper_bound(tail, ub2) or not head <= ub2 or List.cons(head, tail) != List.cons(head, tail) or is_upper_bound(List.cons(head, tail), ub2)","not is_upper_bound(tail, ub2) or not head <= ub2 or is_upper_bound(List.cons(head, tail), ub2)"]}
{"goal":"p(List.cons(head, tail))","proof":["not is_upper_bound(List.cons(head, tail), ub2) or p(List.cons(head, tail))","is_upper_bound(List.cons(head, tail), ub1) or p(List.cons(head, tail))","is_upper_bound(List.cons(head, tail), ub1)","not is_upper_bound(List.cons(head, tail), ub2)","is_upper_bound(List.cons(head, tail), ub2)"]}
{"goal":"upper_bound_monotone","proof":["let (s0: T, s1: List[T]) satisfy { forall(x0: List[T]) { not p(List.nil[T]) or p(s1) or p(x0) } and forall(x1: List[T]) { not p(List.cons(s0, s1)) or not p(List.nil[T]) or p(x1) } }","not p(List.nil[T]) or p(s1) or p(s1)","p(s1) or p(s1)","not p(List.cons(s0, s1)) or not p(List.nil[T]) or p(list)","not p(List.cons(s0, s1)) or p(list)","not p(s1) or p(List.cons(s0, s1))","not ub1 <= ub2 or not is_upper_bound(list, ub1) or not p(list) or is_upper_bound(list, ub2)","not is_upper_bound(list, ub1) or not p(list) or is_upper_bound(list, ub2)","p(s1)","not p(list)","not p(List.cons(s0, s1))"]}
{"goal":"list_max(h, List.nil[T]) = h","proof":["List.nil[T] != List.nil[T] or list_max(h, List.nil[T]) = h"]}
{"goal":"is_upper_bound(List.cons(h, List.nil[T]), h)","proof":["not is_upper_bound(List.nil[T], h) or not h <= h or List.cons(h, List.nil[T]) != List.cons(h, List.nil[T]) or is_upper_bound(List.cons(h, List.nil[T]), h)","h <= h or h <= h","List.nil[T] != List.nil[T] or is_upper_bound(List.nil[T], h)","is_upper_bound(List.nil[T], h)","h <= h","not is_upper_bound(List.nil[T], h) or not h <= h or is_upper_bound(List.cons(h, List.nil[T]), h)"]}
{"goal":"p(List.nil[T])","proof":["let s0: List[T] -> T satisfy { forall(x0: List[T], x1: T) { not p(x0) or is_upper_bound(List.cons(x1, x0), list_max(x1, x0)) } and forall(x2: List[T]) { not is_upper_bound(List.cons(s0(x2), x2), list_max(s0(x2), x2)) or p(x2) } }","is_upper_bound(List.cons(list_max(s0(List.nil[T]), List.nil[T]), List.nil[T]), list_max(s0(List.nil[T]), List.nil[T]))","not is_upper_bound(List.cons(s0(List.nil[T]), List.nil[T]), list_max(s0(List.nil[T]), List.nil[T])) or p(List.nil[T])","List.nil[T] != List.nil[T] or list_max(s0(List.nil[T]), List.nil[T]) = s0(List.nil[T])","list_max(s0(List.nil[T]), List.nil[T]) = s0(List.nil[T])","not is_upper_bound(List.cons(s0(List.nil[T]), List.nil[T]), list_max(s0(List.nil[T]), List.nil[T]))"]}
{"goal":"is_upper_bound(List.cons(next, rest), list_max(next, rest))","proof":["not p(rest) or is_upper_bound(List.cons(next, rest), list_max(next, rest))"]}
{"goal":"m = h.max(list_max(next, rest))","proof":["List.cons(next, rest) != List.cons(next, rest) or h.max(list_max(next, rest)) = list_max(h, List.cons(next, rest))","h.max(list_max(next, rest)) != list_max(h, List.cons(next, rest))"]}
{"goal":"h <= m","proof":["m >= h = h <= m","not m >= h","h.max(list_max(next, rest)) >= h"]}
{"goal":"list_max(next, rest) <= m","proof":["m >= list_max(next, rest) = list_max(next, rest) <= m","not m >= list_max(next, rest)","h.max(list_max(next, rest)) >= list_max(next, rest)"]}
{"goal":"is_upper_bound(List.cons(next, rest), m)","proof":["not is_upper_bound(List.cons(next, rest), list_max(next, rest)) or not list_max(next, rest) <= m or is_upper_bound(List.cons(next, rest), m)","not is_upper_bound(List.cons(next, rest), list_max(next, rest)) or not list_max(next, rest) <= m"]}
{"goal":"is_upper_bound(List.cons(h, List.cons(next, rest)), m)","proof":["not is_upper_bound(List.cons(next, rest), m) or not h <= m or List.cons(h, List.cons(next, rest)) != List.cons(h, List.cons(next, rest)) or is_upper_bound(List.cons(h, List.cons(next, rest)), m)","not is_upper_bound(List.cons(next, rest), m) or not h <= m or is_upper_bound(List.cons(h, List.cons(next, rest)), m)"]}
{"goal":"is_upper_bound(List.cons(h, List.cons(next, rest)), list_max(h, List.cons(next, rest)))","proof":["not is_upper_bound(List.cons(h, List.cons(next, rest)), m)"]}
{"goal":"is_upper_bound(List.cons(h, List.cons(next, rest)), list_max(h, List.cons(next, rest)))","proof":["is_upper_bound(List.cons(h, List.cons(next, rest)), list_max(h, List.cons(next, rest)))"]}
{"goal":"p(List.cons(next, rest))","proof":["let s0: List[T] -> T satisfy { forall(x0: List[T], x1: T) { not p(x0) or is_upper_bound(List.cons(x1, x0), list_max(x1, x0)) } and forall(x2: List[T]) { not is_upper_bound(List.cons(s0(x2), x2), list_max(s0(x2), x2)) or p(x2) } }","is_upper_bound(List.cons(s0(List.cons(next, rest)), List.cons(next, rest)), list_max(s0(List.cons(next, rest)), List.cons(next, rest)))","not is_upper_bound(List.cons(s0(List.cons(next, rest)), List.cons(next, rest)), list_max(s0(List.cons(next, rest)), List.cons(next, rest))) or p(List.cons(next, rest))"]}
{"goal":"p(tail)","proof":["let (s0: T, s1: List[T]) satisfy { forall(x0: List[T]) { not p(List.nil[T]) or p(s1) or p(x0) } and forall(x1: List[T]) { not p(List.cons(s0, s1)) or not p(List.nil[T]) or p(x1) } }","not p(List.nil[T]) or p(s1) or p(tail)","p(s1) or p(tail)","p(s1)","not p(List.cons(s0, s1)) or not p(List.nil[T]) or p(tail)","not p(List.cons(s0, s1)) or p(tail)","not p(s1) or p(List.cons(s0, s1))","not p(List.cons(s0, s1))"]}
{"goal":"is_upper_bound(List.cons(head, tail), list_max(head, tail))","proof":["not p(tail) or is_upper_bound(List.cons(head, tail), list_max(head, tail))"]}
{"goal":"list_max_is_upper_bound","proof":[]}
{"goal":"is_upper_bound(List.cons(head, tail), ub)","proof":["is_upper_bound(List.cons(head, tail), list_max(head, tail))"]}
{"goal":"list_has_upper_bound","proof":["not is_upper_bound(List.cons(head, tail), ub)"]}
{"goal":"p(List.nil[T])","proof":["c ∈ List.nil[T] or p(List.nil[T])","c ∈ List.nil[T]","not c ∈ List.nil[T] or List.nil[T] != List.nil[T]"]}
{"goal":"lb <= head","proof":["not is_lower_bound(List.cons(head, tail), lb) or List.cons(head, tail) != List.cons(head, tail) or lb <= head","not is_lower_bound(List.cons(head, tail), lb) or List.cons(head, tail) != List.cons(head, tail)","not is_lower_bound(List.cons(head, tail), lb)"]}
{"goal":"is_lower_bound(tail, lb)","proof":["not is_lower_bound(List.cons(head, tail), lb) or List.cons(head, tail) != List.cons(head, tail) or is_lower_bound(tail, lb)","not is_lower_bound(List.cons(head, tail), lb) or List.cons(head, tail) != List.cons(head, tail)","not is_lower_bound(List.cons(head, tail), lb)"]}
{"goal":"lb <= c","proof":[]}
{"goal":"tail.contains(c)","proof":["not c ∈ List.cons(head, tail) or List.cons(head, tail) != List.cons(head, tail) or c ∈ tail or head = c","not c ∈ List.cons(head, tail) or List.cons(head, tail) != List.cons(head, tail) or head = c","List.cons(head, tail) != List.cons(head, tail) or not c ∈ List.cons(head, tail)"]}
{"goal":"lb <= c","proof":["not is_lower_bound(tail, lb) or not c ∈ tail or not p(tail) or lb <= c","not is_lower_bound(tail, lb) or not c ∈ tail or not p(tail)","not p(tail)"]}
{"goal":"lb <= c","proof":[]}
{"goal":"p(List.cons(head, tail))","proof":["not lb <= c or p(List.cons(head, tail))","not lb <= c","c ∈ List.cons(head, tail) or p(List.cons(head, tail))","is_lower_bound(List.cons(head, tail), lb) or p(List.cons(head, tail))","is_lower_bound(List.cons(head, tail), lb)","c ∈ List.cons(head, tail)","not c ∈ List.cons(head, tail)"]}
{"goal":"lower_bound_contains","proof":["not is_lower_bound(list, lb) or not c ∈ list or not p(list) or lb <= c","not is_lower_bound(list, lb) or not c ∈ list or not p(list)","let (s0: T, s1: List[T]) satisfy { forall(x0: List[T]) { not p(List.nil[T]) or p(s1) or p(x0) } and forall(x1: List[T]) { not p(List.cons(s0, s1)) or not p(List.nil[T]) or p(x1) } }","not p(List.nil[T]) or p(s1) or p(s1)","p(s1) or p(s1)","not p(List.cons(s0, s1)) or not p(List.nil[T]) or p(list)","not p(List.cons(s0, s1)) or p(list)","not p(s1) or p(List.cons(s0, s1))","p(s1)","not p(list)","not p(List.cons(s0, s1))"]}
{"goal":"p(List.nil[T])","proof":["not is_lower_bound(List.nil[T], lb) or p(List.nil[T])","not is_lower_bound(List.nil[T], lb)","List.nil[T] != List.nil[T] or is_lower_bound(List.nil[T], lb)"]}
{"goal":"List.cons(head, tail) + list2 = List.cons(head, tail + list2)","proof":["List.cons(head, tail) != List.cons(head, tail) or List.cons(head, tail) + list2 = List.cons(head, tail + list2)"]}
{"goal":"is_lower_bound(List.cons(head, tail + list2), lb)","proof":[]}
{"goal":"lb <= head","proof":["not is_lower_bound(List.cons(head, tail) + list2, lb) or List.cons(head, tail) + list2 != List.cons(head, tail + list2) or lb <= head","not is_lower_bound(List.cons(head, tail) + list2, lb) or List.cons(head, tail) + list2 != List.cons(head, tail + list2)"]}
{"goal":"is_lower_bound(tail + list2, lb)","proof":["not is_lower_bound(List.cons(head, tail) + list2, lb) or List.cons(head, tail) + list2 != List.cons(head, tail + list2) or is_lower_bound(tail + list2, lb)","List.cons(head, tail) + list2 != List.cons(head, tail + list2) or not is_lower_bound(List.cons(head, tail) + list2, lb)"]}
{"goal":"is_lower_bound(tail, lb)","proof":["not is_lower_bound(tail + list2, lb) or not p(tail) or is_lower_bound(tail, lb)"]}
{"goal":"is_lower_bound(List.cons(head, tail), lb)","proof":["not is_lower_bound(tail, lb) or not lb <= head or List.cons(head, tail) != List.cons(head, tail) or is_lower_bound(List.cons(head, tail), lb)","not is_lower_bound(tail, lb) or not lb <= head or is_lower_bound(List.cons(head, tail), lb)"]}
{"goal":"p(List.cons(head, tail))","proof":["not is_lower_bound(List.cons(head, tail), lb) or p(List.cons(head, tail))","is_lower_bound(List.cons(head, tail) + list2, lb) or p(List.cons(head, tail))","not is_lower_bound(List.cons(head, tail), lb)","is_lower_bound(List.cons(head, tail) + list2, lb)","not is_lower_bound(List.cons(head, tail) + list2, lb)"]}
{"goal":"lower_bound_add_left","proof":["let (s0: T, s1: List[T]) satisfy { forall(x0: List[T]) { not p(List.nil[T]) or p(s1) or p(x0) } and forall(x1: List[T]) { not p(List.cons(s0, s1)) or not p(List.nil[T]) or p(x1) } }","not p(List.nil[T]) or p(s1) or p(s1)","p(s1) or p(s1)","not p(List.cons(s0, s1)) or not p(List.nil[T]) or p(list1)","not p(List.cons(s0, s1)) or p(list1)","not p(s1) or p(List.cons(s0, s1))","not is_lower_bound(list1 + list2, lb) or not p(list1) or is_lower_bound(list1, lb)","p(s1)","not p(list1)","not p(List.cons(s0, s1))"]}
{"goal":"List.nil[T] + list2 = list2","proof":["List.nil[T] != List.nil[T] or List.nil[T] + list2 = list2"]}
{"goal":"p(List.nil[T])","proof":["not is_lower_bound(list2, lb) or p(List.nil[T])","not is_lower_bound(list2, lb)","is_lower_bound(List.nil[T] + list2, lb) or p(List.nil[T])","not is_lower_bound(List.nil[T] + list2, lb)","is_lower_bound(List.nil[T] + list2, lb)"]}
{"goal":"List.cons(head, tail) + list2 = List.cons(head, tail + list2)","proof":["List.cons(head, tail) != List.cons(head, tail) or List.cons(head, tail) + list2 = List.cons(head, tail + list2)"]}
{"goal":"is_lower_bound(List.cons(head, tail + list2), lb)","proof":[]}
{"goal":"is_lower_bound(tail + list2, lb)","proof":["not is_lower_bound(List.cons(head, tail) + list2, lb) or List.cons(head, tail) + list2 != List.cons(head, tail + list2) or is_lower_bound(tail + list2, lb)","List.cons(head, tail) + list2 != List.cons(head, tail + list2) or not is_lower_bound(List.cons(head, tail) + list2, lb)"]}
{"goal":"is_lower_bound(list2, lb)","proof":["not is_lower_bound(tail + list2, lb) or not p(tail) or is_lower_bound(list2, lb)","not is_lower_bound(tail + list2, lb) or not p(tail)","not is_lower_bound(tail + list2, lb)"]}
{"goal":"p(List.cons(head, tail))","proof":["not is_lower_bound(list2, lb) or p(List.cons(head, tail))","not is_lower_bound(list2, lb)","is_lower_bound(List.cons(head, tail) + list2, lb) or p(List.cons(head, tail))"]}
{"goal":"lower_bound_add_right","proof":["not is_lower_bound(list1 + list2, lb) or not p(list1) or is_lower_bound(list2, lb)","not is_lower_bound(list1 + list2, lb) or not p(list1)","let (s0: T, s1: List[T]) satisfy { forall(x0: List[T]) { not p(List.nil[T]) or p(s1) or p(x0) } and forall(x1: List[T]) { not p(List.cons(s0, s1)) or not p(List.nil[T]) or p(x1) } }","not p(List.nil[T]) or p(s1) or p(s1)","p(s1) or p(s1)","not p(List.cons(s0, s1)) or not p(List.nil[T]) or p(list1)","not p(List.cons(s0, s1)) or p(list1)","not p(s1) or p(List.cons(s0, s1))","p(s1)","not p(list1)","not p(List.cons(s0, s1))"]}
{"goal":"p(List.nil[T])","proof":["not is_lower_bound(List.nil[T], lb2) or p(List.nil[T])","not is_lower_bound(List.nil[T], lb2)","List.nil[T] != List.nil[T] or is_lower_bound(List.nil[T], lb2)"]}
{"goal":"lb1 <= head","proof":["not is_lower_bound(List.cons(head, tail), lb1) or List.cons(head, tail) != List.cons(head, tail) or lb1 <= head","not is_lower_bound(List.cons(head, tail), lb1) or List.cons(head, tail) != List.cons(head, tail)","not is_lower_bound(List.cons(head, tail), lb1)"]}
{"goal":"is_lower_bound(tail, lb1)","proof":["not is_lower_bound(List.cons(head, tail), lb1) or List.cons(head, tail) != List.cons(head, tail) or is_lower_bound(tail, lb1)","not is_lower_bound(List.cons(head, tail), lb1) or List.cons(head, tail) != List.cons(head, tail)","not is_lower_bound(List.cons(head, tail), lb1)"]}
{"goal":"lb2 <= head","proof":["not lib(util).is_transitive[T](T.lte) or not lb2 <= lb1 or not lb1 <= head or lb2 <= head","not lb1 <= head or not lb2 <= lb1"]}
{"goal":"is_lower_bound(tail, lb2)","proof":["not lb2 <= lb1 or not is_lower_bound(tail, lb1) or not p(tail) or is_lower_bound(tail, lb2)","not is_lower_bound(tail, lb1) or not p(tail) or is_lower_bound(tail, lb2)"]}
{"goal":"is_lower_bound(List.cons(head, tail), lb2)","proof":["not is_lower_bound(tail, lb2) or not lb2 <= head or List.cons(head, tail) != List.cons(head, tail) or is_lower_bound(List.cons(head, tail), lb2)","not is_lower_bound(tail, lb2) or not lb2 <= head or is_lower_bound(List.cons(head, tail), lb2)"]}
{"goal":"p(List.cons(head, tail))","proof":["not is_lower_bound(List.cons(head, tail), lb2) or p(List.cons(head, tail))","is_lower_bound(List.cons(head, tail), lb1) or p(List.cons(head, tail))","is_lower_bound(List.cons(head, tail), lb1)","not is_lower_bound(List.cons(head, tail), lb2)","is_lower_bound(List.cons(head, tail), lb2)"]}
{"goal":"lower_bound_monotone","proof":["let (s0: T, s1: List[T]) satisfy { forall(x0: List[T]) { not p(List.nil[T]) or p(s1) or p(x0) } and forall(x1: List[T]) { not p(List.cons(s0, s1)) or not p(List.nil[T]) or p(x1) } }","not p(List.nil[T]) or p(s1) or p(s1)","p(s1) or p(s1)","not p(List.cons(s0, s1)) or not p(List.nil[T]) or p(list)","not p(List.cons(s0, s1)) or p(list)","not p(s1) or p(List.cons(s0, s1))","not lb2 <= lb1 or not is_lower_bound(list, lb1) or not p(list) or is_lower_bound(list, lb2)","not is_lower_bound(list, lb1) or not p(list) or is_lower_bound(list, lb2)","p(s1)","not p(list)","not p(List.cons(s0, s1))"]}
{"goal":"list_min(h, List.nil[T]) = h","proof":["List.nil[T] != List.nil[T] or list_min(h, List.nil[T]) = h"]}
{"goal":"is_lower_bound(List.cons(h, List.nil[T]), h)","proof":["not is_lower_bound(List.nil[T], h) or not h <= h or List.cons(h, List.nil[T]) != List.cons(h, List.nil[T]) or is_lower_bound(List.cons(h, List.nil[T]), h)","h <= h or h <= h","List.nil[T] != List.nil[T] or is_lower_bound(List.nil[T], h)","is_lower_bound(List.nil[T], h)","h <= h","not is_lower_bound(List.nil[T], h) or not h <= h or is_lower_bound(List.cons(h, List.nil[T]), h)"]}
{"goal":"p(List.nil[T])","proof":["let s0: List[T] -> T satisfy { forall(x0: List[T], x1: T) { not p(x0) or is_lower_bound(List.cons(x1, x0), list_min(x1, x0)) } and forall(x2: List[T]) { not is_lower_bound(List.cons(s0(x2), x2), list_min(s0(x2), x2)) or p(x2) } }","is_lower_bound(List.cons(list_min(s0(List.nil[T]), List.nil[T]), List.nil[T]), list_min(s0(List.nil[T]), List.nil[T]))","not is_lower_bound(List.cons(s0(List.nil[T]), List.nil[T]), list_min(s0(List.nil[T]), List.nil[T])) or p(List.nil[T])","List.nil[T] != List.nil[T] or list_min(s0(List.nil[T]), List.nil[T]) = s0(List.nil[T])","list_min(s0(List.nil[T]), List.nil[T]) = s0(List.nil[T])","not is_lower_bound(List.cons(s0(List.nil[T]), List.nil[T]), list_min(s0(List.nil[T]), List.nil[T]))"]}
{"goal":"is_lower_bound(List.cons(next, rest), list_min(next, rest))","proof":["not p(rest) or is_lower_bound(List.cons(next, rest), list_min(next, rest))"]}
{"goal":"m = h.min(list_min(next, rest))","proof":["List.cons(next, rest) != List.cons(next, rest) or h.min(list_min(next, rest)) = list_min(h, List.cons(next, rest))","h.min(list_min(next, rest)) != list_min(h, List.cons(next, rest))"]}
{"goal":"m <= h","proof":["h.min(list_min(next, rest)) <= h"]}
{"goal":"m <= list_min(next, rest)","proof":["h.min(list_min(next, rest)) <= list_min(next, rest)"]}
{"goal":"is_lower_bound(List.cons(next, rest), m)","proof":["not is_lower_bound(List.cons(next, rest), list_min(next, rest)) or not m <= list_min(next, rest) or is_lower_bound(List.cons(next, rest), m)","not is_lower_bound(List.cons(next, rest), list_min(next, rest)) or not m <= list_min(next, rest)"]}
{"goal":"is_lower_bound(List.cons(h, List.cons(next, rest)), m)","proof":["not is_lower_bound(List.cons(next, rest), m) or not m <= h or List.cons(h, List.cons(next, rest)) != List.cons(h, List.cons(next, rest)) or is_lower_bound(List.cons(h, List.cons(next, rest)), m)","not is_lower_bound(List.cons(next, rest), m) or not m <= h or is_lower_bound(List.cons(h, List.cons(next, rest)), m)"]}
{"goal":"is_lower_bound(List.cons(h, List.cons(next, rest)), list_min(h, List.cons(next, rest)))","proof":["not is_lower_bound(List.cons(h, List.cons(next, rest)), m)"]}
{"goal":"is_lower_bound(List.cons(h, List.cons(next, rest)), list_min(h, List.cons(next, rest)))","proof":["is_lower_bound(List.cons(h, List.cons(next, rest)), list_min(h, List.cons(next, rest)))"]}
{"goal":"p(List.cons(next, rest))","proof":["let s0: List[T] -> T satisfy { forall(x0: List[T], x1: T) { not p(x0) or is_lower_bound(List.cons(x1, x0), list_min(x1, x0)) } and forall(x2: List[T]) { not is_lower_bound(List.cons(s0(x2), x2), list_min(s0(x2), x2)) or p(x2) } }","is_lower_bound(List.cons(s0(List.cons(next, rest)), List.cons(next, rest)), list_min(s0(List.cons(next, rest)), List.cons(next, rest)))","not is_lower_bound(List.cons(s0(List.cons(next, rest)), List.cons(next, rest)), list_min(s0(List.cons(next, rest)), List.cons(next, rest))) or p(List.cons(next, rest))"]}
{"goal":"p(tail)","proof":["let (s0: T, s1: List[T]) satisfy { forall(x0: List[T]) { not p(List.nil[T]) or p(s1) or p(x0) } and forall(x1: List[T]) { not p(List.cons(s0, s1)) or not p(List.nil[T]) or p(x1) } }","not p(List.nil[T]) or p(s1) or p(tail)","p(s1) or p(tail)","p(s1)","not p(List.cons(s0, s1)) or not p(List.nil[T]) or p(tail)","not p(List.cons(s0, s1)) or p(tail)","not p(s1) or p(List.cons(s0, s1))","not p(List.cons(s0, s1))"]}
{"goal":"is_lower_bound(List.cons(head, tail), list_min(head, tail))","proof":["not p(tail) or is_lower_bound(List.cons(head, tail), list_min(head, tail))"]}
{"goal":"list_min_is_lower_bound","proof":[]}
{"goal":"is_lower_bound(List.cons(head, tail), lb)","proof":["is_lower_bound(List.cons(head, tail), list_min(head, tail))"]}
{"goal":"list_has_lower_bound","proof":["not is_lower_bound(List.cons(head, tail), lb)"]}
