{"goal":"(reverse[T](tail) + List.singleton(head)).length = reverse[T](tail).length + List.singleton(head).length","proof":["reverse[T](tail).length + List.singleton(head).length = (reverse[T](tail) + List.singleton(head)).length"]}
{"goal":"p(List.cons(head, tail))","proof":["reverse[T](List.cons(head, tail)).length != List.cons(head, tail).length or p(List.cons(head, tail))","not p(tail) or reverse[T](tail).length = tail.length","reverse[T](tail) + List.singleton(head) = reverse[T](tail).append(head)","reverse[T](tail).length + List.singleton(head).length = List.singleton(head).length + reverse[T](tail).length","List.cons(head, List.nil[T]) = List.singleton(head)","1 + tail.length = tail.length.suc","List.cons(head, List.nil[T]).length = List.nil[T].length.suc","List.cons(head, tail).length = tail.length.suc","reverse[T](tail).append(head) = reverse[T](List.cons(head, tail))"]}
{"goal":"reverse_length","proof":["let (s0: T, s1: List[T]) satisfy { forall(x0: List[T]) { not p(List.nil[T]) or p(s1) or p(x0) } and forall(x1: List[T]) { not p(List.cons(s0, s1)) or not p(List.nil[T]) or p(x1) } }","not p(List.nil[T]) or p(s1) or p(list)","not p(s1) or p(List.cons(s0, s1))","not p(list) or reverse[T](list).length = list.length","not p(List.cons(s0, s1)) or not p(List.nil[T]) or p(list)","reverse[T](List.nil[T]) != List.nil[T] or p(List.nil[T])"]}
{"goal":"p(List.nil[T])","proof":["reverse[T](list2) + reverse[T](List.nil[T]) != reverse[T](List.nil[T] + list2) or p(List.nil[T])","List.nil[T] + list2 = list2","reverse[T](list2) + List.nil[T] = reverse[T](list2)"]}
{"goal":"reverse[T](List.cons(head, tail) + list2) = reverse[T](list2) + reverse[T](List.cons(head, tail))","proof":["not p(tail) or reverse[T](list2) + reverse[T](tail) = reverse[T](tail + list2)","reverse[T](list2) + (reverse[T](tail) + List.singleton(head)) = reverse[T](list2) + reverse[T](tail) + List.singleton(head)","reverse[T](tail + list2) + List.singleton(head) = reverse[T](tail + list2).append(head)","reverse[T](tail) + List.singleton(head) = reverse[T](tail).append(head)","reverse[T](tail + list2).append(head) = reverse[T](List.cons(head, tail + list2))","reverse[T](tail).append(head) = reverse[T](List.cons(head, tail))","List.cons(head, tail) + list2 = List.cons(head, tail + list2)"]}
{"goal":"reverse_add","proof":["let (s0: T, s1: List[T]) satisfy { forall(x0: List[T]) { not p(List.nil[T]) or p(s1) or p(x0) } and forall(x1: List[T]) { not p(List.cons(s0, s1)) or not p(List.nil[T]) or p(x1) } }","not p(List.nil[T]) or p(s1) or p(s1)","not p(List.cons(s0, s1)) or not p(List.nil[T]) or p(list1)","reverse[T](list2) + reverse[T](List.cons(s0, s1)) != reverse[T](List.cons(s0, s1) + list2) or p(List.cons(s0, s1))","not p(list1) or reverse[T](list2) + reverse[T](list1) = reverse[T](list1 + list2)","not p(s1) or reverse[T](list2) + reverse[T](List.cons(s0, s1)) = reverse[T](List.cons(s0, s1) + list2)"]}
{"goal":"p(List.nil[T])","proof":["reverse[T](reverse[T](List.nil[T])) != List.nil[T] or p(List.nil[T])"]}
{"goal":"reverse[T](List.singleton(head)) + reverse[T](reverse[T](tail)) = List.singleton(head) + tail","proof":["not p(tail) or reverse[T](reverse[T](tail)) = tail","List.nil[T] + List.singleton(head) = List.nil[T].append(head)","List.cons(head, List.nil[T]) = List.singleton(head)","List.nil[T] + List.singleton(head) = List.singleton(head)","reverse[T](List.nil[T]).append(head) = reverse[T](List.cons(head, List.nil[T]))"]}
{"goal":"p(List.cons(head, tail))","proof":["reverse[T](reverse[T](List.cons(head, tail))) != List.cons(head, tail) or p(List.cons(head, tail))","reverse[T](List.singleton(head)) + reverse[T](reverse[T](tail)) = reverse[T](reverse[T](tail) + List.singleton(head))","reverse[T](tail) + List.singleton(head) = reverse[T](tail).append(head)","List.cons(head, List.nil[T]) = List.singleton(head)","List.nil[T] + tail = tail","reverse[T](tail).append(head) = reverse[T](List.cons(head, tail))","List.cons(head, List.nil[T]) + tail = List.cons(head, List.nil[T] + tail)"]}
{"goal":"reverse_involution","proof":["let (s0: T, s1: List[T]) satisfy { forall(x0: List[T]) { not p(List.nil[T]) or p(s1) or p(x0) } and forall(x1: List[T]) { not p(List.cons(s0, s1)) or not p(List.nil[T]) or p(x1) } }","not p(List.nil[T]) or p(s1) or p(s1)","not p(List.cons(s0, s1)) or not p(List.nil[T]) or p(list)","not p(s1) or p(List.cons(s0, s1))","not p(list) or reverse[T](reverse[T](list)) = list"]}
{"goal":"reverse[T](List.cons(head, tail)).contains(item) = (reverse[T](tail) + List.singleton(head)).contains(item)","proof":["List.cons(head, tail) != List.cons(head, tail) or reverse[T](tail).append(head) = reverse[T](List.cons(head, tail))","reverse[T](tail) + List.singleton(head) = reverse[T](tail).append(head)"]}
{"goal":"List.cons(head, tail).contains(item)","proof":["not p(tail) or item ∈ reverse[T](tail) = item ∈ tail","not item ∈ reverse[T](tail) + List.cons(head, List.nil[T]) or item ∈ reverse[T](tail) or item ∈ List.cons(head, List.nil[T])","List.cons(head, tail) != List.cons(head, tail) or head != item or item ∈ List.cons(head, tail)","not item ∈ List.cons(head, List.nil[T]) or item ∈ List.cons(head, List.nil[T]) + tail","List.cons(head, List.nil[T]) = List.singleton(head)","List.nil[T] + tail = tail","List.cons(head, List.nil[T]) + tail = List.cons(head, List.nil[T] + tail)","not item ∈ tail or item ∈ List.cons(head, tail) or head = item"]}
{"goal":"(reverse[T](tail) + List.singleton(head)).contains(item)","proof":["not item ∈ List.singleton(head) or item ∈ reverse[T](tail) + List.singleton(head)","not item ∈ reverse[T](tail) or item ∈ reverse[T](tail) + List.singleton(head)","List.cons(item, List.nil[T]) = List.singleton(item)","not item ∈ tail or not p(tail) or item ∈ reverse[T](tail)","not item ∈ List.cons(head, tail) or item ∈ tail or head = item","item ∈ List.cons(item, List.nil[T])"]}
{"goal":"(reverse[T](tail) + List.singleton(head)).contains(item) = List.cons(head, tail).contains(item)","proof":["item ∈ reverse[T](tail) + List.singleton(head) or item ∈ List.cons(head, tail)","not item ∈ reverse[T](tail) + List.singleton(head) or not item ∈ List.cons(head, tail)","item ∈ reverse[T](tail) + List.singleton(head)","not item ∈ reverse[T](tail) + List.singleton(head)"]}
{"goal":"reverse[T](List.cons(head, tail)).contains(item) = List.cons(head, tail).contains(item)","proof":["item ∈ reverse[T](tail) + List.singleton(head) != item ∈ reverse[T](List.cons(head, tail))"]}
{"goal":"p(List.cons(head, tail))","proof":["item ∈ reverse[T](List.cons(head, tail)) != item ∈ List.cons(head, tail) or p(List.cons(head, tail))"]}
{"goal":"reverse_contains","proof":["let (s0: T, s1: List[T]) satisfy { forall(x0: List[T]) { not p(List.nil[T]) or p(s1) or p(x0) } and forall(x1: List[T]) { not p(List.cons(s0, s1)) or not p(List.nil[T]) or p(x1) } }","not p(List.nil[T]) or p(s1) or p(list)","not p(s1) or p(List.cons(s0, s1))","not p(List.cons(s0, s1)) or not p(List.nil[T]) or p(list)","not p(list) or item ∈ reverse[T](list) = item ∈ list","reverse[T](List.nil[T]) != List.nil[T] or p(List.nil[T])"]}
{"goal":"p(List.nil[T])","proof":["idx < List.nil[T].length or p(List.nil[T])","not idx < 0"]}
{"goal":"idx < reverse[T](tail).length","proof":["reverse[T](tail).length = tail.length"]}
{"goal":"tail.length.suc - 1 - idx = tail.length - idx","proof":["tail.length.suc - 1 != tail.length","tail.length + 1 - 1 = tail.length","tail.length + 1 = tail.length.suc"]}
{"goal":"tail.length - idx > 0","proof":["not tail.length > idx or tail.length - idx > 0","tail.length > idx = idx < tail.length","not tail.length > idx","tail.length > idx"]}
{"goal":"List.cons(head, tail).get_idx(tail.length - idx) = tail.get_idx(tail.length - idx - 1)","proof":["not tail.length - idx > 0 or List.cons(head, tail) != List.cons(head, tail) or List.cons(head, tail).get_idx(tail.length - idx) = tail.get_idx(tail.length - idx - 1)","List.cons(head, tail) != List.cons(head, tail)"]}
{"goal":"tail.get_idx(tail.length - idx - 1) = tail.get_idx(tail.length - 1 - idx)","proof":["not 1 + idx <= tail.length or tail.length - idx - 1 = tail.length - 1 - idx","not idx < tail.length or idx.suc <= tail.length","1 + idx = idx.suc"]}
{"goal":"List.cons(head, tail).get_idx(tail.length.suc - 1 - idx) = tail.get_idx(tail.length - 1 - idx)","proof":["not tail.length > idx or tail.length - idx > 0","tail.length > idx = idx < tail.length","tail.length.suc - 1 = tail.length","not tail.length - idx > 0 or List.cons(head, tail).get_idx(tail.length - idx) = tail.get_idx(tail.length - idx - 1)"]}
{"goal":"reverse[T](tail).get_idx(idx) = tail.get_idx(tail.length - 1 - idx)","proof":["not idx < tail.length or not p(tail) or tail.get_idx(tail.length - 1 - idx) = reverse[T](tail).get_idx(idx)"]}
{"goal":"(reverse[T](tail) + List.singleton(head)).get_idx(idx) = reverse[T](tail).get_idx(idx)","proof":["not idx < reverse[T](tail).length or (reverse[T](tail) + List.singleton(head)).get_idx(idx) = reverse[T](tail).get_idx(idx)"]}
{"goal":"List.cons(head, tail).length - 1 - idx = tail.length.suc - 1 - idx","proof":["List.cons(head, tail).length - 1 != tail.length.suc - 1","List.cons(head, tail) != List.cons(head, tail) or List.cons(head, tail).length = tail.length.suc","List.cons(head, tail).length != tail.length.suc","List.cons(head, tail).length = tail.length.suc"]}
{"goal":"List.cons(head, tail).get_idx(List.cons(head, tail).length - 1 - idx) = tail.get_idx(tail.length - 1 - idx)","proof":["List.cons(head, tail).get_idx(tail.length.suc - 1 - idx) != tail.get_idx(tail.length - 1 - idx)"]}
{"goal":"reverse[T](List.cons(head, tail)).get_idx(idx) = List.cons(head, tail).get_idx(List.cons(head, tail).length - 1 - idx)","proof":["not idx < tail.length or not p(tail) or tail.get_idx(tail.length - 1 - idx) = reverse[T](tail).get_idx(idx)","not idx < reverse[T](tail).length or (reverse[T](tail) + List.singleton(head)).get_idx(idx) = reverse[T](tail).get_idx(idx)","reverse[T](tail) + List.singleton(head) = reverse[T](tail).append(head)","List.cons(head, tail).length = tail.length.suc","reverse[T](tail).append(head) = reverse[T](List.cons(head, tail))"]}
{"goal":"p(List.cons(head, tail))","proof":["List.cons(head, tail).get_idx(List.cons(head, tail).length - 1 - idx) != reverse[T](List.cons(head, tail)).get_idx(idx) or p(List.cons(head, tail))","List.cons(head, tail).get_idx(List.cons(head, tail).length - 1 - idx) != reverse[T](List.cons(head, tail)).get_idx(idx)"]}
{"goal":"(reverse[T](tail) + List.singleton(head)).get_idx(tail.length) = Option.some(head)","proof":["(reverse[T](tail) + List.singleton(head)).get_idx(reverse[T](tail).length) = Option.some(head)","reverse[T](tail).length = tail.length"]}
{"goal":"List.cons(head, tail).get_idx(0) = Option.some(head)","proof":["List.cons(head, tail) != List.cons(head, tail) or 0 > 0 or List.cons(head, tail).get_idx(0) = Option.some(head)","0 > 0 = 0 < 0","not 0 < 0"]}
{"goal":"tail.length - tail.length = 0","proof":["0 + tail.length != tail.length or tail.length - tail.length = 0","0 + tail.length = tail.length"]}
{"goal":"reverse[T](List.cons(head, tail)).get_idx(idx) = List.cons(head, tail).get_idx(List.cons(head, tail).length - 1 - idx)","proof":["not idx < tail.length.suc or idx < tail.length or tail.length = idx","reverse[T](tail) + List.singleton(head) = reverse[T](tail).append(head)","idx.min(tail.length) = tail.length or idx < tail.length","idx.min(tail.length).suc - 1 = idx.min(tail.length)","List.cons(head, tail).length = tail.length.suc","reverse[T](tail).append(head) = reverse[T](List.cons(head, tail))"]}
{"goal":"p(List.cons(head, tail))","proof":["List.cons(head, tail).get_idx(List.cons(head, tail).length - 1 - idx) != reverse[T](List.cons(head, tail)).get_idx(idx) or p(List.cons(head, tail))","List.cons(head, tail).get_idx(List.cons(head, tail).length - 1 - idx) != reverse[T](List.cons(head, tail)).get_idx(idx)"]}
{"goal":"p(List.cons(head, tail))","proof":["sum[A](reverse[A](List.cons(head, tail))) != sum[A](List.cons(head, tail)) or p(List.cons(head, tail))","not p(tail) or sum[A](reverse[A](tail)) = sum[A](tail)","sum[A](reverse[A](tail).append(head)) = sum[A](reverse[A](tail)) + head","sum[A](tail) + head = head + sum[A](tail)","reverse[A](tail).append(head) = reverse[A](List.cons(head, tail))","head + sum[A](tail) = sum[A](List.cons(head, tail))"]}
{"goal":"p(List.cons(head, tail))","proof":["idx < List.cons(head, tail).length or p(List.cons(head, tail))"]}
{"goal":"reverse_get_idx","proof":["let (s0: T, s1: List[T]) satisfy { forall(x0: List[T]) { not p(List.nil[T]) or p(s1) or p(x0) } and forall(x1: List[T]) { not p(List.cons(s0, s1)) or not p(List.nil[T]) or p(x1) } }","not p(List.nil[T]) or p(s1) or p(s1)","p(s1) or p(s1)","not p(List.cons(s0, s1)) or not p(List.nil[T]) or p(list)","not p(List.cons(s0, s1)) or p(list)","not p(s1) or p(List.cons(s0, s1))","p(s1)","not idx < list.length or not p(list) or list.get_idx(list.length - 1 - idx) = reverse[T](list).get_idx(idx)","not p(list)","not p(List.cons(s0, s1))"]}
{"goal":"n.range.get_idx(n - 1 - idx) = Option.some(n - 1 - idx)","proof":["not n - 1 - idx < n or n.range.get_idx(n - 1 - idx) = Option.some(n - 1 - idx)","0 < n or n < 0 or n = 0","n > 0 = 0 < n","not n < 0"]}
{"goal":"map[Nat, Nat](n.range, function(x0: Nat) { n - 1 - x0 }).get_idx(idx) = Option.some(n - 1 - idx)","proof":["let s0: Nat -> Nat satisfy { forall(x0: Nat) { n - 1 - x0 = s0(x0) } }","n - 1 - idx = s0(idx)","not idx < n or map[Nat, Nat](n.range, s0).get_idx(idx) = Option.some(s0(idx))"]}
{"goal":"reverse[Nat](n.range).get_idx(idx) = map[Nat, Nat](n.range, function(x0: Nat) { n - 1 - x0 }).get_idx(idx)","proof":["not idx < n.range.length or n.range.get_idx(n.range.length - 1 - idx) = reverse[Nat](n.range).get_idx(idx)","n.range.length = n"]}
{"goal":"range_reverse_map_get_idx","proof":["not idx < 0 or idx <= 0","not idx < 0 or idx != 0","not idx <= 0 or idx = 0"]}
{"goal":"n.range.length = n","proof":["n.range.length = n"]}
{"goal":"map[Nat, Nat](n.range, function(x0: Nat) { n - 1 - x0 }).length = n.range.length","proof":["let s0: Nat -> Nat satisfy { forall(x0: Nat) { n - 1 - x0 = s0(x0) } }","map[Nat, Nat](n.range, s0).length = n.range.length"]}
{"goal":"reverse[Nat](n.range).get_idx(idx) = map[Nat, Nat](n.range, function(x0: Nat) { n - 1 - x0 }).get_idx(idx)","proof":["reverse[Nat](n.range).length = n.range.length"]}
{"goal":"reverse[Nat](n.range).length = map[Nat, Nat](n.range, function(x0: Nat) { n - 1 - x0 }).length","proof":["let s0: Nat -> Nat satisfy { forall(x0: Nat) { n - 1 - x0 = s0(x0) } }","reverse[Nat](n.range).length = n.range.length"]}
{"goal":"reverse[Nat](n.range) = map[Nat, Nat](n.range, function(x0: Nat) { n - 1 - x0 })","proof":["let s0: Nat -> Nat satisfy { forall(x0: Nat) { n - 1 - x0 = s0(x0) } }","let (s1: Nat -> Nat, s2: Nat, s3: Nat -> Nat, s4: Nat -> Nat) satisfy { map[Nat, Nat](n.range, s0).length != reverse[Nat](n.range).length or s2 < reverse[Nat](n.range).length or map[Nat, Nat](n.range, s0) = reverse[Nat](n.range) and (map[Nat, Nat](n.range, s0).get_idx(s2) != reverse[Nat](n.range).get_idx(s2) or map[Nat, Nat](n.range, s0).length != reverse[Nat](n.range).length or map[Nat, Nat](n.range, s0) = reverse[Nat](n.range)) }","n - 1 - s2 = s0(s2)","let s5: (Nat, Nat) -> Nat satisfy { forall(x1: Nat, x2: Nat) { n - 1 - x1 = s5(x2, x1) } }","n - 1 - s2 = s5(s2, s2)","not s2 < reverse[Nat](n.range).length or map[Nat, Nat](n.range, s5(s2)).get_idx(s2) = reverse[Nat](n.range).get_idx(s2)","not s2 < n or map[Nat, Nat](n.range, s0).get_idx(s2) = Option.some(s0(s2))","not s2 < n or map[Nat, Nat](n.range, s5(s2)).get_idx(s2) = Option.some(s5(s2, s2))"]}
{"goal":"range_reverse_map","proof":["let s0: Nat -> Nat satisfy { forall(x0: Nat) { n - 1 - x0 = s0(x0) } }","map[Nat, Nat](List.nil[Nat], s0) = List.nil[Nat]"]}
{"goal":"p(List.cons(head, tail))","proof":["sum[A](reverse[A](List.cons(head, tail))) != sum[A](List.cons(head, tail)) or p(List.cons(head, tail))","not p(tail) or sum[A](reverse[A](tail)) = sum[A](tail)","List.cons(head, tail) != List.cons(head, tail) or sum[A](List.cons(head, tail)) = head + sum[A](tail)","List.cons(head, tail) != List.cons(head, tail) or reverse[A](List.cons(head, tail)) = reverse[A](tail).append(head)","sum[A](reverse[A](tail).append(head)) = sum[A](reverse[A](tail)) + head","head + sum[A](tail) = sum[A](tail) + head","reverse[A](List.cons(head, tail)) = reverse[A](tail).append(head)","sum[A](List.cons(head, tail)) = head + sum[A](tail)","sum[A](reverse[A](List.cons(head, tail))) != sum[A](List.cons(head, tail))","sum[A](reverse[A](tail)) = sum[A](tail)"]}
{"goal":"sum_reverse","proof":["let (s0: A, s1: List[A]) satisfy { forall(x0: List[A]) { not p(List.nil[A]) or p(s1) or p(x0) } and forall(x1: List[A]) { not p(List.cons(s0, s1)) or not p(List.nil[A]) or p(x1) } }","not p(List.nil[A]) or p(s1) or p(list)","not p(s1) or p(List.cons(s0, s1))","not p(list) or sum[A](reverse[A](list)) = sum[A](list)","not p(List.cons(s0, s1)) or not p(List.nil[A]) or p(list)","reverse[A](List.nil[A]) != List.nil[A] or p(List.nil[A])"]}
