{"goal":"Real.one_half > 0","proof":["function[T0: lib(order).PartialOrder](x0: T0, x1: T0) { (not x0 <= x1 and x1 <= x0) = x0 > x1 }[Real](Real.one_half, 0)","function[T0: lib(order).PartialOrder](x0: T0, x1: T0) { (not x0 <= x1 and x1 <= x0) = x1 < x0 }[Real](Real.one_half, 0)"]}
{"goal":"rpow(0, Real.one_half) = Option.some(0)","proof":[]}
{"goal":"sqrt(0) = Option.some(0)","proof":["function(x0: Real) { rpow(x0, Real.one_half) = sqrt(x0) }(0)"]}
{"goal":"sqrt_zero","proof":[]}
{"goal":"exists(k0: Real) { rpow(x, Real.one_half) = Option.some(k0) and k0 > 0 }","proof":[]}
{"goal":"exists(k0: Real) { rpow(x, Real.one_half) = Option.some(k0) and k0 > 0 }","proof":[]}
{"goal":"sqrt(x) = Option.some(y)","proof":["function(x0: Real) { rpow(x0, Real.one_half) = sqrt(x0) }(x)"]}
{"goal":"exists(k0: Real) { k0 = y and sqrt(x) = Option.some(k0) and k0 > 0 }","proof":[]}
{"goal":"sqrt_pos","proof":["function(x0: Real) { not (sqrt(x) = Option.some(x0) and x0 > 0) }(y)"]}
{"goal":"sqrt(x) = Option.some(y)","proof":["function(x0: Real) { rpow(x0, Real.one_half) = sqrt(x0) }(x)","function(x0: Real, x1: Real) { (if x0 > 0 { Option.some(exp(x1 * log_or_zero(x0))) } else { if x0 = 0 { if x1 = 0 { Option.some(1) } else { if x1 > 0 { Option.some(0) } else { Option.none[Real] } } } else { Option.none[Real] } }) = rpow(x0, x1) }(x, Real.one_half)","Option.some(exp(Real.one_half * log_or_zero(x))) != sqrt(x)","function(x0: Real, x1: Real) { not x0 > 0 or Option.some(exp(x1 * log_or_zero(x0))) = rpow(x0, x1) }(x, Real.one_half)","Option.some(exp(Real.one_half * log_or_zero(x))) != rpow(x, Real.one_half)"]}
{"goal":"exp(Real.one_half * log_or_zero(x) + Real.one_half * log_or_zero(x)) = y * y","proof":["exp(Real.one_half * log_or_zero(x) + Real.one_half * log_or_zero(x)) != exp(Real.one_half * log_or_zero(x)) * y","exp(Real.one_half * log_or_zero(x) + Real.one_half * log_or_zero(x)) = exp(Real.one_half * log_or_zero(x)) * y"]}
{"goal":"Real.one_half * log_or_zero(x) + Real.one_half * log_or_zero(x) = (Real.one_half + Real.one_half) * log_or_zero(x)","proof":["function(x0: Real, x1: Real, x2: Real) { x0 * x1 + x2 * x1 = (x0 + x2) * x1 }(Real.one_half, log_or_zero(x), Real.one_half)"]}
{"goal":"Real.one_half + Real.one_half = 1","proof":[]}
{"goal":"(Real.one_half + Real.one_half) * log_or_zero(x) = 1 * log_or_zero(x)","proof":["Real.one_half + Real.one_half != 1"]}
{"goal":"1 * log_or_zero(x) = log_or_zero(x)","proof":["function(x0: Real) { 1 * x0 = x0 }(log_or_zero(x))"]}
{"goal":"Real.one_half * log_or_zero(x) + Real.one_half * log_or_zero(x) = log_or_zero(x)","proof":["Real.one_half * log_or_zero(x) + Real.one_half * log_or_zero(x) != 1 * log_or_zero(x)","Real.one_half * log_or_zero(x) + Real.one_half * log_or_zero(x) = 1 * log_or_zero(x)"]}
{"goal":"y * y = exp(log_or_zero(x))","proof":["exp(Real.one_half * log_or_zero(x) + Real.one_half * log_or_zero(x)) != y * y"]}
{"goal":"exp(log_or_zero(x)) = x","proof":["not x > 0"]}
{"goal":"y * y = x","proof":[]}
{"goal":"exists(k0: Real) { k0 = y and sqrt(x) = Option.some(k0) and k0 * k0 = x }","proof":[]}
{"goal":"exists(k0: Real) { sqrt(x) = Option.some(k0) and k0 * k0 = x }","proof":["function(x0: Real) { not (sqrt(x) = Option.some(x0) and x0 * x0 = x) }(y)"]}
{"goal":"x > 0","proof":[]}
{"goal":"false","proof":[]}
{"goal":"x <= 0","proof":[]}
{"goal":"0 <= x","proof":["function[T0: lib(order).PartialOrder](x0: T0, x1: T0) { x0 >= x1 = x1 <= x0 }[Real](x, 0)"]}
{"goal":"0 = x","proof":[]}
{"goal":"x = 0","proof":[]}
{"goal":"sqrt(x) = Option.some(0)","proof":[]}
{"goal":"0 * 0 = 0","proof":["function(x0: Real) { 0 * x0 = 0 }(0)"]}
{"goal":"exists(k0: Real) { k0 = 0 and sqrt(x) = Option.some(k0) and k0 * k0 = x }","proof":[]}
{"goal":"exists(k0: Real) { sqrt(x) = Option.some(k0) and k0 * k0 = x }","proof":["0 = x","function(x0: Real) { not (sqrt(x) = Option.some(x0) and x0 * x0 = x) }(0)"]}
{"goal":"sqrt_mul_self","proof":[]}
{"goal":"x.is_positive","proof":["function(x0: Real) { not x0 > 0 or x0.is_positive }(x)"]}
{"goal":"(x * x).is_positive","proof":["function(x0: Real, x1: Real) { not x0.is_positive or not x1.is_positive or (x0 * x1).is_positive }(x, x)"]}
{"goal":"x * x > 0","proof":["function(x0: Real) { not x0.is_positive or x0 > 0 }(x * x)"]}
{"goal":"sqrt(x * x) = Option.some(exp(Real.one_half * log_or_zero(x * x)))","proof":["function(x0: Real) { rpow(x0, Real.one_half) = sqrt(x0) }(x * x)","function(x0: Real, x1: Real) { (if x0 > 0 { Option.some(exp(x1 * log_or_zero(x0))) } else { if x0 = 0 { if x1 = 0 { Option.some(1) } else { if x1 > 0 { Option.some(0) } else { Option.none[Real] } } } else { Option.none[Real] } }) = rpow(x0, x1) }(x * x, Real.one_half)","function(x0: Real, x1: Real) { not x0 > 0 or Option.some(exp(x1 * log_or_zero(x0))) = rpow(x0, x1) }(x * x, Real.one_half)","Option.some(exp(Real.one_half * log_or_zero(x * x))) != rpow(x * x, Real.one_half)"]}
{"goal":"log(x * x) = Option.some(log_or_zero(x) + log_or_zero(x))","proof":["not x > 0"]}
{"goal":"log(x * x) = Option.some(log_or_zero(x * x))","proof":["function(x0: Real) { not x0 > 0 or Option.some(log_or_zero(x0)) = log(x0) }(x * x)"]}
{"goal":"Option.some(log_or_zero(x * x)) = Option.some(log_or_zero(x) + log_or_zero(x))","proof":["Option.some(log_or_zero(x) + log_or_zero(x)) != log(x * x)"]}
{"goal":"log_or_zero(x * x) = log_or_zero(x) + log_or_zero(x)","proof":["Option.some(log_or_zero(x) + log_or_zero(x)) != Option.some(log_or_zero(x * x))"]}
{"goal":"Real.one_half * log_or_zero(x * x) = Real.one_half * (log_or_zero(x) + log_or_zero(x))","proof":["log_or_zero(x) + log_or_zero(x) != log_or_zero(x * x)"]}
{"goal":"Real.one_half * (log_or_zero(x) + log_or_zero(x)) = Real.one_half * log_or_zero(x) + Real.one_half * log_or_zero(x)","proof":["function(x0: Real, x1: Real, x2: Real) { x0 * x1 + x0 * x2 = x0 * (x1 + x2) }(Real.one_half, log_or_zero(x), log_or_zero(x))"]}
{"goal":"Real.one_half * log_or_zero(x) + Real.one_half * log_or_zero(x) = (Real.one_half + Real.one_half) * log_or_zero(x)","proof":["function(x0: Real, x1: Real, x2: Real) { x0 * x1 + x2 * x1 = (x0 + x2) * x1 }(Real.one_half, log_or_zero(x), Real.one_half)"]}
{"goal":"Real.one_half + Real.one_half = 1","proof":[]}
{"goal":"(Real.one_half + Real.one_half) * log_or_zero(x) = 1 * log_or_zero(x)","proof":["Real.one_half + Real.one_half != 1"]}
{"goal":"1 * log_or_zero(x) = log_or_zero(x)","proof":["function(x0: Real) { 1 * x0 = x0 }(log_or_zero(x))"]}
{"goal":"Real.one_half * log_or_zero(x * x) = log_or_zero(x)","proof":[]}
{"goal":"exp(Real.one_half * log_or_zero(x * x)) = exp(log_or_zero(x))","proof":["Real.one_half * log_or_zero(x * x) != log_or_zero(x)"]}
{"goal":"exp(log_or_zero(x)) = x","proof":["not x > 0"]}
{"goal":"sqrt(x * x) = Option.some(x)","proof":[]}
{"goal":"x > 0","proof":[]}
{"goal":"false","proof":[]}
{"goal":"x <= 0","proof":[]}
{"goal":"0 <= x","proof":["function[T0: lib(order).PartialOrder](x0: T0, x1: T0) { x0 >= x1 = x1 <= x0 }[Real](x, 0)"]}
{"goal":"0 = x","proof":[]}
{"goal":"x = 0","proof":[]}
{"goal":"x * x = 0","proof":["function(x0: Real) { 0 * x0 = 0 }(x)"]}
{"goal":"sqrt(x * x) = Option.some(0)","proof":[]}
{"goal":"sqrt(x * x) = Option.some(x)","proof":[]}
{"goal":"sqrt_square_of_nonneg","proof":[]}
{"goal":"sqrt(y * y) = Option.some(y)","proof":[]}
{"goal":"sqrt(x) = sqrt(y * y)","proof":[]}
{"goal":"sqrt(x) = Option.some(y)","proof":[]}
{"goal":"sqrt_unique_nonneg","proof":[]}
