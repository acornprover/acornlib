# Topology

Goal: provide the general topological language that analysis, geometry, and probability need.

- [ ] Migrate the existing `is_open_in_subspace` / `is_closed_in_subspace` predicate-style API and its lemmas onto the new `Subspace[T, a]: TopologicalSpace` carrier (now in `src/topological_space.ac`), then delete the predicates. The carrier-side inclusion map `subspace_inclusion` and its continuity `subspace_inclusion_continuous` are now in `src/subspace_inclusion.ac` (isolated module to keep proof search fast; the generic helper `set_preimage_contains_image` was added to `set.ac`). Still blocked on `borel.ac`, which imports `is_open_in_subspace` / `is_closed_in_subspace` directly — migrate those Borel-subspace measurability lemmas to the carrier before deleting the predicates. Keep subspace migration proofs small and cache-stable.
- [ ] Migrate the remaining `is_open_in_product` consumers onto the `Pair[X, Y]: TopologicalSpace` carrier instance. The carrier instance, its four open-set axioms (`product_open_empty/universal/inter/big_union`), and the bridge `pair_is_open_eq` are in `src/product_open_axioms.ac`. Product-topology properties through the generic API are now done: `box(u, v)` open for open factors (`box_is_box` / `box_generated_open` / `box_is_open_in_product` isolated in `src/product_open_axioms.ac`'s helper module `src/product_box_open.ac` to dodge the higher-order `is_box` exists-intro / generated-open blowup, then `box_open` on the carrier), and both projections continuous (`pair_fst`/`pair_snd`, with `pair_fst_preimage_eq`/`pair_snd_preimage_eq` showing the preimage of a factor open is a box, and `pair_fst_continuous`/`pair_snd_continuous`; the `is_continuous` define needed an explicit forall-unfold to close). The bridge `box_eq_inter_preimages` (`box(u, v) = fst_preimage(u) ∩ snd_preimage(v)`) is also in place. The fan-out map `fan_out(f, g)(z) = (f(z), g(z))` (in `src/pair.ac`) and its API are now in `src/product_map.ac`: `pair_fst_compose_fan_out`/`pair_snd_compose_fan_out` (the projections recover the components), `fan_out_preimage_box` (`set_preimage(fan_out(f, g), box(u, v)) = set_preimage(f, u) ∩ set_preimage(g, v)`). Box algebra is in `src/product_box_algebra.ac`: `box_inter` (`box(u1, v1) ∩ box(u2, v2) = box(u1 ∩ u2, v1 ∩ v2)`, so boxes are closed under finite intersection — a basis property) and `box_empty_left`/`box_empty_right`. (`box_mono` was attempted but its subset-membership step times out the prover; deferred.) The box step `fan_out_preimage_box_open` (`is_continuous(f) and is_continuous(g) and X.is_open(u) and Y.is_open(v) implies Z.is_open(set_preimage(fan_out(f, g), box(u, v)))`) is now DONE in `src/product_map.ac`, assembled from `fan_out_preimage_box` + `open_inter` + the new `continuous_open_preimage`. The earlier blocker was the basic fact `is_continuous(f) and Y.is_open(v) implies X.is_open(set_preimage(f, v))`: its `is_continuous` define-unfold forall-instantiation times out at the default 5s outside `topological_space.ac`, and putting it beside the definition poisons the premise pool (breaks `continuous_imp_continuous_at`). Fix: an isolated minimal-premise module `src/continuous_preimage.ac` holding just `continuous_open_preimage`; the instantiation IS findable, it just needs a longer search (~45s) once, and the resulting cached certificate then replays instantly under `acorn check` (the codebase's standard committed-cert pattern). Consumers cite the lemma (one-step MP), dodging the blowup. Next: lift to all product-opens (`fan_out` continuous everywhere) via the union-of-basis characterization (`generated_open_iff_union_of_basis` + preimage-commutes-with-`big_union` + `big_union` of opens open), then prove the product topology coarsest making both projections continuous.
- [ ] Build out the separability/countability API: `is_countable[T](s)` is in `src/countable.ac` (now meaning `s = ∅` or `s` is covered by a sequence, with `empty_set_is_countable`, `countable_has_sequence_of_nonempty`, `subset_of_sequence_covered_is_countable`, `image_of_sequence_covered_is_countable`, and `nat_universal_is_countable`) and `is_subspace_separable[T](s)` (`exists(d) { d.subset(s) and is_countable(d) and s.subset(closure(d)) }`) is in `src/separable.ac`. Next: add wrapper closure lemmas such as subset/image closure for `is_countable`, then finite-set/binary-union closure and an `is_subspace_separable_imp_*` link to second-countability once a basis API exists.
- [ ] Develop the compactness API (universal-set is compact, finite-union of compacts is compact, image of compact under continuous is compact, closed subset of compact is compact). The per-cover lemma `singleton_open_cover_imp_finite_subcover` is in place, and `singleton_is_compact` is now PROVED (`src/topological_space.ac`). The earlier block — a forward `forall(c) { is_open_cover implies exists(items){...} }` block assembled from the per-cover lemma — does time out, because the final `... = is_compact[T](s)` definitional unfold (matching the cover-dependent existential under the `forall`) overruns proof search; note `empty_set_is_compact` only passes on its cached certificate and also times out under `--force-search`. The fix was to write the proof as an **explicit refutation** instead of a forward assembly: cite the unfolding lemma `is_compact_iff[T](s)`, then `if not is_compact[T](s) { let w satisfy { is_open_cover[T](w, s) and not exists(items){...} }; <cite the per-cover lemma at w>; <restate the existential>; false }`. Skolemizing the negation by hand and citing the per-cover lemma at that witness keeps every sub-goal inside the 5s budget (4 fast searches). This refutation pattern is the likely route past the sibling blockers below (connectedness conjunct extraction, product-map continuity) — both are the same "definitional unfold / higher-order match times out under a forward proof" shape. Next: `universal_set_is_compact`, finite-union, continuous image, closed-subset-of-compact.
- [x] The equivalence of `is_connected` and `is_connected_via_clopen` is DONE in `src/connectedness.ac`: forward `connected_imp_via_clopen` (with `compl_empty_imp_universal` in `src/compl_universal.ac`) and reverse `via_clopen_imp_connected`. The reverse direction's two earlier blockers were both beaten by avoiding higher-order `open` goals: (1) **field extraction** — `is_disconnection[T](open, u, v) implies open(u)` was unblocked by establishing the full unfolded conjunction as a fact, then `if not open(u) { not (conjunction); false }` (a direct P-and-not-P contradiction, so `open(u)` is only ever a hypothesis, never a goal); helpers `disconnection_imp_open_left`/`_right`, `disconnection_imp_fields`, `disconnection_left_clopen` package this. (2) **clopen-forall instantiation** — never prove the `or` consequent `s = empty or s = universal` as a goal (the prover backward-searches a disjunct and wanders). Instead `via_clopen_nonempty_clopen_universal` concludes the clean equality `s = universal` with the contradiction inside `if s != universal { false }`, so the prover derives the `or` as a *forward* fact and refutes it against the inequalities. Generic set lemmas `compl_eq_of_partition`/`compl_eq_of_disjoint_cover` are in `src/compl_partition.ac`. Acorn parses `and`/`or` at equal precedence, left-associative, so parenthesize mixed boolean expressions.
- [ ] Add path connectedness (needs the unit interval and continuous paths)
- [ ] Build continuity in terms of filter convergence (the pointwise neighborhood-based variant `is_continuous_at` and the equivalence `continuous_iff_continuous_at` are in place; the closed-preimage equivalence `continuous_iff_preimage_closed` is now in `src/continuity_closed.ac` — isolated module to keep proof search fast; the filter-convergence equivalence remains)
- [ ] Define embeddings (an injective continuous map that is a homeomorphism onto its image; needs the subspace topology big-union axiom)
- [ ] Add topological groups, rings, and modules
- [ ] Prove foundational results such as Baire-category style theorems
- [ ] Generate a topology from a subbasis: finite intersections of subbasis members form a basis. `list_inter_of_sets` and its nil/cons unfolding lemmas are in place, and `is_subfamily_nil` / `is_subfamily_cons_mp` / `is_subfamily_cons_mpr` provide the subfamily unfoldings; `list_inter_concat` and `is_subfamily_concat` proofs by list induction time out the prover even with explicit step-by-step witnesses (the base+cons-step pattern that succeeds for `add_contains_left` does not close here), so either decompose further or take an axiom shortcut next

Note: families of sets are still modelled as `Set[T] -> Bool` predicates and consumed
via `big_union` / `big_intersection`; revisit direct `Set[Set[T]]` collections
only when a migration has a concrete payoff.
