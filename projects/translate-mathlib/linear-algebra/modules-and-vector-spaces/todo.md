# Modules and Vector Spaces

Goal: introduce a usable Module/VectorSpace API that later linear-algebra
work (linear maps, bases, matrices, etc.) can build on.

## Settled Design

- Encoding: bundled `Module[R: Ring, M: AddCommGroup]` structure with `smul: R -> M -> M`
  field, mirroring the `GroupHom[G, H]` two-parameter pattern. The four axioms are split
  into named sub-predicates (`module_smul_add_left_constraint`, `..._add_right_constraint`,
  `..._assoc_constraint`, `..._one_constraint`) combined by `is_module_action`.
- Spelling: `m.smul(r, x)` (typeclass `*` cannot be heterogeneous over two type
  parameters in Acorn).
- `VectorSpace` is a thin convention: a `Module[F, V]` with `F: Field`, not a separate
  structure. Field-specific lemmas live in `src/vector_space.ac`.
- Linear maps: predicate-level only (`preserves_add`, `preserves_smul`, `is_linear_map`)
  in `src/module_hom.ac`, plus `trivial_linear_map`, `identity_fn_is_linear_map`,
  and composition lemmas. Add a bundled `ModuleHom[R, M, N]` when the predicate-level
  API needs a bundled wrapper.
- Linear equivalences: predicate-level (`is_linear_equiv` in `src/module_hom.ac`), the
  pair-based `is_linear_equiv_pair`/`modules_linearly_equivalent` relation, and the bundled
  `LinearEquiv[R, M, N]` structure, all in `src/module_iso.ac`. `LinearEquiv` bundles `src`,
  `dst`, `to_fn`, `inv_fn` with a forall-only constraint
  (`is_linear_map(src, dst, to_fn) and is_two_sided_inverse_fn(to_fn, inv_fn)`, mirroring
  Mathlib's `to_fun`/`inv_fun`/`left_inv`/`right_inv`). The forall-only constraint sidesteps the
  earlier failure (constraining with `is_linear_equiv`, whose surjectivity unfolds to an
  existential, timed out on field extraction). Known limitation: extracting the concrete
  forward/inverse *value* (`.to_fn`/`.inv_fn`) from a *specific constructed* equivalence
  (`linear_equiv_identity`/`_symm`/`_compose`) times out — the two-function-field relational
  constraint blows up field extraction even in an isolated minimal module (a `ModuleHom`-style
  single-function-field struct does not). Endpoint (`.src`/`.dst`) projections work; generic
  per-`e` lemmas work.

## Implementation Tasks

- [ ] Package the submodule quotient as a `Module[R, QuotientOver[M]]` (axioms via
      representatives, using `submodule_quotient_add/neg/zero/smul_by`). Note:
      requires first equipping `QuotientOver[M]` with an `AddCommGroup` instance,
      which the existing add-subgroup quotient also does not yet provide; the
      four module axioms are already verified at the representative level
      (`submodule_quotient_smul_add_left/right`, `submodule_quotient_smul_assoc`,
      `submodule_quotient_smul_one`).
- [ ] Promote the canonical projection `submodule_quotient_mk` to a linear map via
      `is_linear_map` once `Module[R, QuotientOver[M]]` is available; the
      representative-level intertwining lemmas
      (`submodule_quotient_mk_add`, `submodule_quotient_mk_neg`,
      `submodule_quotient_mk_smul`, `submodule_quotient_mk_zero`) are in place.
- [ ] Promote the kernel-of-canonical-projection-equals-submodule statement to the
      bundled-submodule level once `Module[R, QuotientOver[M]]` is available; the
      representative-level kernel characterisation
      (`submodule_quotient_mk_eq_zero_iff_in_submodule`) is in place.
- [ ] Extend packaged kernel/image submodule APIs only when downstream formalizations need additional lemmas.
- [ ] Extend the packaged `linear_map_preimage_submodule` API only when downstream formalizations need additional lemmas (e.g. monotonicity in the target submodule, agreement with `linear_map_kernel_submodule` on the zero submodule, or full-target characterisation).
- [ ] Build on the bundled `LinearEquiv[R, M, N]` / `modules_linearly_equivalent` relation in `src/module_iso.ac`: transport submodule/kernel/image structure across a linear equivalence. Done: `linear_equiv_of_module_hom` builds a bundled `LinearEquiv` from a `ModuleHom` plus a two-sided inverse of its underlying function, with existence (`..._some`) and `.src`/`.dst` projection lemmas; `src/complex_conj_module_hom.ac` adds the concrete bundled instance `complex_conj_linear_equiv` (conjugation as a real-linear self-equivalence of `Complex`) with `..._some`/`.src`/`.dst` lemmas. `src/linear_equiv_submodule.ac` (isolated module) lifts the kernel/image submodule characterisations to a bundled `LinearEquiv`: `linear_equiv_bundled_kernel_submodule_contains_iff_zero`, `linear_equiv_bundled_image_submodule_contains`, and pointwise agreement with the zero/full submodule (`..._kernel_submodule_contains_eq_zero_submodule`, `..._image_submodule_contains_eq_full_submodule`); plus the inverse-direction analogues (`linear_equiv_bundled_inv_kernel_submodule_contains_iff_zero`, `..._inv_image_submodule_contains`, and their zero/full pointwise-agreement forms) via `linear_equiv_inv_fn_is_linear_equiv`. Forward- and inverse-direction subset-style inclusions are also packaged: `linear_equiv_bundled_kernel_submodule_subset_zero_submodule`, `zero_submodule_subset_linear_equiv_bundled_kernel_submodule`, `linear_equiv_bundled_image_submodule_subset_full_submodule`, `full_submodule_subset_linear_equiv_bundled_image_submodule`, and their inverse-direction analogues (`linear_equiv_bundled_inv_kernel_submodule_subset_zero_submodule`, `zero_submodule_subset_linear_equiv_bundled_inv_kernel_submodule`, `linear_equiv_bundled_inv_image_submodule_subset_full_submodule`, `full_submodule_subset_linear_equiv_bundled_inv_image_submodule`), built atop predicate-level companions in `src/submodule.ac` (`linear_equiv_kernel_submodule_subset_zero_submodule`, `zero_submodule_subset_linear_equiv_kernel_submodule`, `full_submodule_subset_linear_equiv_image_submodule`). Full submodule-level equalities are also packaged via `submodule_subset_antisymm`: `linear_equiv_bundled_kernel_submodule_eq_zero_submodule`, `linear_equiv_bundled_image_submodule_eq_full_submodule`, and the inverse-direction analogues `linear_equiv_bundled_inv_kernel_submodule_eq_zero_submodule`, `linear_equiv_bundled_inv_image_submodule_eq_full_submodule`.
- [ ] prover-limitation (narrow): `linear_equiv_identity`/`_symm`/`_compose` in `src/module_iso.ac` lack `.to_fn`/`.inv_fn` value projections — extracting a constrained function field from a constructed value times out for `LinearEquiv`'s two-function-field relational constraint. Revisit if the prover's field-extraction improves; until then use the constructors' defining `satisfy` property or the generic per-`e` lemmas.

Status:

- `src/module_hom.ac` adds predicate-level linear-map convenience lemmas: `preserves_add_zero`, `linear_map_zero`, `linear_map_smul` (single-argument scalar preservation), `linear_map_add` (single-pair addition preservation), `linear_map_neg`, and `linear_map_sub`.
- `src/module_hom.ac` defines pointwise `neg_fn` (and reuses `add_fn` from `add_semigroup.ac`) at the predicate level, with `add_fn_preserves_add`/`add_fn_preserves_smul`/`add_fn_is_linear_map` and `neg_fn_preserves_add`/`neg_fn_preserves_smul`/`neg_fn_is_linear_map` showing that pointwise sums and negations of linear maps are linear.
- `src/module_hom.ac` defines pointwise `sub_fn` and proves `sub_fn_eq_add_neg`, `sub_fn_preserves_add`, `sub_fn_preserves_smul`, and `sub_fn_is_linear_map`. It also adds the additive-group identity/inverse laws on the linear-map function space at the predicate level: `add_fn_trivial_linear_map_left/right` and `add_fn_neg_fn`.
- `src/vector_space.ac` adds field-specific lemmas: `vector_space_smul_ne_zero` (nonzero scalar times nonzero vector is nonzero), `vector_space_smul_inverse_left/right` (multiplying by inverse undoes scalar multiplication), `vector_space_smul_left_cancel`/`vector_space_smul_right_cancel` (cancellation of nonzero scalar/vector), `vector_space_smul_eq_implies_eq_inverse_smul`/`vector_space_inverse_smul_implies_smul_eq` (solving `smul(r, v) = u` via the inverse scalar), `vector_space_smul_eq_iff_left_cancel`/`vector_space_smul_eq_iff_right_cancel` (iff forms of the cancellation laws), `vector_space_smul_eq_zero_iff` (bidirectional zero characterisation), `vector_space_smul_ne_zero_iff` (bidirectional nonzero characterisation), `vector_space_smul_eq_iff_eq_inverse_smul` (iff form of inverse-scalar solving), and `vector_space_neg_smul_eq_smul_neg`.
- `src/submodule.ac` defines `submodule_zero_constraint`, `submodule_add_constraint`, `submodule_neg_constraint`, `submodule_smul_constraint`, `is_submodule`, and the bundled `Submodule[R, M]` structure, with extensionality and basic closure lemmas.
- Submodule intersections are represented by `submodule_intersection_contains`, with verified closure lemmas and a combined `submodule_intersection_is_submodule` theorem.
- Linear-map kernels and images are represented by `linear_map_kernel` and `linear_map_image`, with verified zero/add/neg/smul closure lemmas.
- `linear_map_kernel_submodule` and `linear_map_image_submodule` now package predicate-level kernels and images as `Submodule` values, using the full submodule outside the linear-map side condition.
- Packaged kernel/image submodules have verified carrier lemmas, linear-map membership lemmas, kernel introduction/elimination lemmas, image value/elimination lemmas, subset-style universal properties, the trivial-kernel-to-zero-submodule inclusion, and surjectivity/full-image bridges.
- `submodule_rel`, `submodule_quotient_relation`, `submodule_quotient_add/neg/zero`, and `submodule_quotient_smul_by` (left scalar multiplication for a fixed scalar, with projection and representative-compatibility lemmas) provide a representative-level quotient-by-submodule API, delegating additive structure to `submodule_to_add_subgroup` and the existing `add_subgroup` quotient.
- The four module axioms hold for the submodule quotient at the representative level: `submodule_quotient_smul_add_left`, `submodule_quotient_smul_add_right`, `submodule_quotient_smul_assoc`, `submodule_quotient_smul_one`.
- The canonical projection function `submodule_quotient_mk` is defined, with `submodule_quotient_mk_eq_iff_in_submodule`, `submodule_quotient_mk_eq_zero_iff_in_submodule`, `submodule_quotient_mk_zero`, `submodule_quotient_mk_add`, `submodule_quotient_mk_neg`, and `submodule_quotient_mk_smul` providing the kernel characterisation and representative-level linearity.
- `linear_map_preimage` provides predicate-level preimage of a `Submodule[R, N]` under a function `M -> N`, with the four constraint lemmas (`linear_map_preimage_zero/add/neg/smul_constraint`) holding for any `is_linear_map(src, t.carrier, f)`. Bridge lemmas `linear_map_preimage_zero_submodule_eq_kernel` and `linear_map_preimage_full_submodule` connect preimage with the kernel and full submodule.
- `linear_map_preimage_submodule` packages the predicate-level preimage as a `Submodule[R, M]` (mirroring `linear_map_kernel_submodule`), defaulting to the full submodule when the map is not linear. The carrier is the source module and `linear_map_preimage_submodule_contains_eq` characterises membership as `t.contains(f(x))` whenever the map is linear, with introduction/elimination lemmas (`..._contains_of_image_in`, `..._image_in_of_contains`).
- `linear_map_preimage_submodule_full_contains_at` gives the full-target characterisation pointwise: the preimage of `full_submodule(dst)` and `full_submodule(src)` share membership at every point under a linear map. `linear_map_preimage_submodule_mono` gives monotonicity in the target submodule (same `t1.carrier = t2.carrier` and `submodule_subset(t1, t2)` yield subset-inclusion of preimages).
- `linear_map_preimage_submodule_zero_eq_kernel_at` gives the zero-target characterisation pointwise: under a linear map, the preimage submodule of `zero_submodule(dst)` and `linear_map_kernel_submodule(src, dst, f)` share membership at every point.
- `linear_map_preimage_submodule_zero_subset_kernel`, `linear_map_kernel_subset_preimage_submodule_zero`, `linear_map_preimage_submodule_full_subset`, and `full_submodule_subset_linear_map_preimage_full` give subset-style inclusions between the preimage submodule (for the zero/full target submodules) and the kernel/full source submodules under a linear map. The corresponding full submodule equalities still need decomposed `submodule_subset_antisymm` proofs on these large terms.
- `src/submodule.ac` adds bundled-`ModuleHom`-level kernel and image wrappers: `module_hom_kernel(f)` and `module_hom_image(f)` (taking only `f: ModuleHom[R, M, N]`), with carrier lemmas, kernel `contains` characterisation/intro/elim, and the image-contains-value lemma. Additionally provides `module_hom_image_contains_eq` (image-predicate equivalence), `module_hom_image_elim` (preimage existence), `module_hom_image_subset_iff` (image-submodule subset universal property), and `module_hom_kernel_contains_iff_map_eq_zero` (iff form of kernel membership).
- `src/module_hom.ac` adds the bundled `ModuleHom[R, M, N]` structure (fields `src`, `dst`, `hom` with `is_linear_map` constraint), extensionality (`module_hom_ext`), equality projections (`module_hom_eq_src/dst/hom/hom_at`), the linearity projection `module_hom_is_linear_map`, value-level lemmas (`module_hom_zero/add/smul/neg/sub`), the identity hom `module_hom_identity` with projection lemmas (`module_hom_identity_src/dst/hom/hom_at`), and an `ext` attribute alias.
- `src/submodule.ac` adds pointwise kernel/image characterisations under a linear equivalence: `linear_equiv_kernel_iff_zero` (kernel membership iff input is zero) and `linear_equiv_image_full_at` (every destination point lies in the image). Bundled versions `linear_equiv_kernel_submodule_contains_iff_zero` and `linear_equiv_image_submodule_contains` lift these to the packaged kernel/image submodules.
- `src/module_hom.ac` adds the bundled trivial module homomorphism `module_hom_trivial(src, dst)` (constant-zero map) with projection lemmas `module_hom_trivial_hom`, `module_hom_trivial_src`, `module_hom_trivial_dst`, and `module_hom_trivial_hom_at`.
- `src/module_hom.ac` adds bundled-`ModuleHom` identity-composition laws: `module_hom_compose_identity_left_some` and `module_hom_compose_identity_right_some` (composition with the identity hom on either side is defined) together with `module_hom_compose_identity_left_hom` and `module_hom_compose_identity_right_hom` (the resulting hom's underlying function equals the original `f.hom`).
- `src/module_hom.ac` adds the predicate-level lemma `compose_trivial_linear_map_left` (composing the trivial linear map on the left with any function yields the trivial linear map) and the bundled `module_hom_compose_trivial_left_some` / `module_hom_compose_trivial_left_hom` (composing the trivial hom on the destination with any `f: ModuleHom[R, M, N]` is defined, with underlying function `trivial_linear_map[M, K]`).
- `src/module_hom.ac` adds the bundled-`ModuleHom` right trivial-composition laws `module_hom_compose_trivial_right_some` and `module_hom_compose_trivial_right_hom` (composing any `f: ModuleHom[R, N, K]` with the trivial hom `module_hom_trivial(src, f.src)` is defined, with underlying function `trivial_linear_map[M, K]`).
- `src/module_hom.ac` adds `module_hom_compose` for bundled `ModuleHom[R, M, N]` as an `Option`-valued composition (defined exactly when `f.src = g.dst`), with `module_hom_compose_some` (existence under the precondition) and projection lemmas `module_hom_compose_src`, `module_hom_compose_dst`, `module_hom_compose_hom`, and `module_hom_compose_hom_at`.
- `src/submodule.ac` adds bundled `ModuleHom`-level injectivity/surjectivity bridges for the packaged kernel/image submodules: `module_hom_kernel_implies_zero_of_injective` (kernel members of an injective hom are zero), `module_hom_kernel_subset_zero_of_injective` (kernel of an injective hom is contained in the zero submodule), `module_hom_injective_of_kernel_eq_zero` (a hom whose kernel equals the zero submodule is injective on its underlying function), `full_submodule_subset_module_hom_image_of_surjective` (the full destination submodule is contained in the image of a surjective hom), and `module_hom_image_of_image_eq_full` (when the image equals the full submodule, every destination point has a preimage).
- `src/submodule.ac` adds identity-hom kernel/image pointwise lemmas at the bundled-`ModuleHom` level: `module_hom_identity_kernel_contains_eq` (kernel of the identity hom contains a point iff it is zero), `module_hom_identity_kernel_contains_eq_zero_submodule` (pointwise agreement with the zero submodule), `module_hom_identity_image_contains` (every point lies in the image of the identity hom), and `module_hom_identity_image_contains_eq_full_submodule` (pointwise agreement with the full submodule).
- `src/submodule.ac` adds trivial-hom kernel pointwise lemmas: `module_hom_trivial_kernel_contains` (every source point lies in the kernel of the trivial hom) and `module_hom_trivial_kernel_contains_eq_full_submodule` (pointwise agreement with the full source submodule). `src/module_hom_trivial_image.ac` adds the corresponding image lemmas `module_hom_trivial_image_contains_implies_zero`, `module_hom_trivial_image_contains_zero`, `module_hom_trivial_image_contains_eq` (image membership iff equality to zero), and `module_hom_trivial_image_contains_eq_zero_submodule` (pointwise agreement with the zero submodule), isolated in their own module to keep proof search fast.
- `src/submodule.ac` adds bundled-`ModuleHom` kernel/image membership convenience lemmas specializing the generic submodule API: `module_hom_kernel_contains_zero`/`module_hom_image_contains_zero` (the zero element belongs), `module_hom_kernel_contains_add`/`module_hom_image_contains_add` (closure under addition), `module_hom_kernel_contains_neg`/`module_hom_image_contains_neg` (closure under negation), `module_hom_kernel_contains_smul`/`module_hom_image_contains_smul` (closure under the scalar action), and `module_hom_kernel_subset_full`/`module_hom_image_subset_full` (containment in the full source/destination submodule).
- `src/module_hom_kernel_image_eq.ac` (isolated module) adds the bottom/top subset-style inclusions for bundled-`ModuleHom` kernel/image submodules: `zero_submodule_subset_module_hom_kernel` (the source zero submodule is contained in every kernel submodule) and `module_hom_image_subset_full` (the image submodule is contained in the destination full submodule).
- `src/module_hom.ac` introduces a predicate-level linear-equivalence API: `is_linear_equiv` (linear map + bijection), with `linear_equiv_is_linear_map`/`linear_equiv_is_bijection`/`linear_equiv_is_injective`/`linear_equiv_is_surjective` projections, `identity_fn_is_linear_equiv`, `compose_is_linear_equiv`, `linear_map_two_sided_inverse_preserves_add`/`..._preserves_smul`/`..._is_linear_map` (showing any two-sided inverse of a linear map is itself linear in the opposite direction), `linear_equiv_two_sided_inverse_is_linear_equiv`, the convenience lemmas `linear_equiv_zero`/`linear_equiv_add`/`linear_equiv_smul`/`linear_equiv_neg`/`linear_equiv_sub`, the inverse-transport biconditional `linear_equiv_apply_eq_iff` (`f(x) = y iff x = g(y)`), the kernel-triviality biconditional `linear_equiv_apply_eq_zero_iff_eq_zero`, and convenience lemmas `linear_equiv_two_sided_inverse_zero`/`..._add`/`..._smul`/`..._neg`/`..._sub` giving the inverse map's pointwise additive and scalar behavior directly.
- `src/complex_module.ac` packages the complex numbers as a real left module `complex_real_module: Module[Real, Complex]` with scalar action `complex_real_smul`, including the four constraint lemmas (`complex_real_smul_add_left_constraint`, `..._add_right_constraint`, `..._assoc_constraint`, `..._one_constraint`), the combined `complex_real_smul_is_module_action`, and the scalar-action projection `complex_real_module_smul`.
- `src/complex_re_im_linear.ac` verifies that the real-part and imaginary-part functions are real-linear maps from `Complex` (as a real module) to `Real` (as a module over itself): `complex_re_fn_preserves_add`/`..._preserves_smul`/`..._is_linear_map` and the corresponding `complex_im_fn` versions. Both maps are also packaged as bundled `ModuleHom[Real, Complex, Real]` values `complex_re_module_hom`/`complex_im_module_hom`, with existence helpers `complex_re_fn_module_hom_some`/`complex_im_fn_module_hom_some` and source/destination projections.
- `src/complex_from_real_linear.ac` verifies that the real-to-complex embedding `Complex.from_real` is a real-linear map from `Real` (as a module over itself) to `Complex` (as a real module): `complex_from_real_preserves_add`, `complex_from_real_preserves_smul`, and `complex_from_real_is_linear_map`. The map is also packaged as a bundled `ModuleHom[Real, Real, Complex]` via `complex_from_real_module_hom`, with the existence helper `complex_from_real_module_hom_some` and source/destination projections `complex_from_real_module_hom_src` and `complex_from_real_module_hom_dst`.
- `src/complex_conj_linear.ac` adds pointwise compatibility lemmas for complex conjugation with the real scalar action and the additive structure on `Complex`: `complex_conj_fn_real_smul` (conjugation commutes with `complex_real_smul`), `complex_conj_fn_zero`, `complex_conj_fn_neg`, `complex_conj_fn_sub`, and `complex_conj_fn_from_real` (conjugation fixes images of the real-to-complex embedding).
- `src/module_hom_compose_kernel.ac` adds `module_hom_compose_kernel_contains_of_kernel`: an element of the kernel of the inner homomorphism `g` lies in the kernel of a bundled composition `module_hom_compose(f, g)`, plus the subset-style wrapper `module_hom_kernel_inner_subset_module_hom_compose_kernel`, and (when the outer homomorphism is injective) the reverse-direction `module_hom_compose_kernel_contains_implies_kernel_inner_of_injective_outer` with its subset wrapper `module_hom_compose_kernel_subset_kernel_inner_of_injective_outer`. Isolated in its own module to keep proof search fast.
- `src/module_hom_compose_image.ac` adds `module_hom_compose_image_contains_implies_image_outer`: a point in the image of a bundled composition `module_hom_compose(f, g)` lies in the image of the outer homomorphism `f`, plus the subset-style wrapper `module_hom_compose_image_subset_image_outer`. Isolated in its own module to keep proof search fast.
- `src/complex_conj_module_hom.ac` verifies that complex conjugation is a real-linear map from `Complex` (as a real module) to itself: `complex_conj_fn_preserves_add`, `complex_conj_fn_smul_eq`, `complex_conj_fn_preserves_smul`, and `complex_conj_fn_is_linear_map`. The map is packaged as a bundled `ModuleHom[Real, Complex, Complex]` via `complex_conj_module_hom`, with existence helper `complex_conj_fn_module_hom_some` and source/destination projections `complex_conj_module_hom_src`/`complex_conj_module_hom_dst`. It also proves conjugation is a real-linear equivalence: `complex_conj_fn_two_sided_inverse` (conjugation is its own two-sided inverse, via `conj_conj`), `complex_conj_fn_is_bijection`, and `complex_conj_fn_is_linear_equiv`.
- `src/module_iso.ac` actually uses the predicate-level linear-equivalence API to define module isomorphism. It introduces `is_linear_equiv_pair(src, dst, f, g)` (`is_linear_map(src, dst, f) and is_two_sided_inverse_fn(f, g)`, matching the `LinearEquiv` constraint exactly — bijectivity is implied by the two-sided inverse, so it is not stated separately), with projections `linear_equiv_pair_forward`/`linear_equiv_pair_inverse`/`linear_equiv_pair_inverse_is_linear_equiv`, the identity pair `linear_equiv_pair_identity`, reversal `linear_equiv_pair_swap`, and composition `linear_equiv_pair_compose`. It then defines the relation `modules_linearly_equivalent(src, dst)` (existence of such a pair) with introduction lemma `modules_linearly_equivalent_of_pair`, the composition bridges `modules_linearly_equivalent_of_composed_pairs`/`modules_linearly_equivalent_of_pair_trans`, and proves it is an equivalence relation: `modules_linearly_equivalent_refl`, `modules_linearly_equivalent_symm`, `modules_linearly_equivalent_trans`. Bundling the inverse witness inside the existential keeps `symm`/`trans` constructive (no `Inhabited`/choice needed). Note: the existential-introduction step with `compose(...)` witness terms required explicit inline witnesses and explicit hypothesis/goal restatements before each cited lemma to stay inside the prover's search budget.
- `src/complex_conj_module_hom.ac` exercises the `module_iso` API concretely: `complex_conj_fn_is_linear_equiv_pair` packages complex conjugation as a real-linear equivalence pair (its own two-sided inverse), and `complex_real_module_linearly_equivalent_self` concludes that `Complex` as a real module is linearly equivalent to itself via that (non-identity) equivalence.
- `src/module_iso.ac` adds bundled-`LinearEquiv` injectivity/inverse biconditionals: `linear_equiv_to_fn_eq_iff`/`linear_equiv_inv_fn_eq_iff` (the forward/inverse function agrees on two points iff the points are equal), `linear_equiv_inv_apply_eq_iff` (`e.inv_fn(y) = x iff y = e.to_fn(x)`), and `linear_equiv_inv_map_eq_zero_iff_eq_zero` (`e.inv_fn(y) = M.0 iff y = N.0`).
- `src/module_iso.ac` adds bundled-`LinearEquiv` constructor-level `modules_linearly_equivalent` bridges: `linear_equiv_identity_modules_linearly_equivalent` (the identity equivalence witnesses `modules_linearly_equivalent(m, m)`), `linear_equiv_symm_modules_linearly_equivalent` (the inverse equivalence witnesses `modules_linearly_equivalent(e.dst, e.src)`), `linear_equiv_compose_modules_linearly_equivalent` (composing two compatible equivalences witnesses `modules_linearly_equivalent(d.src, e.dst)` when `e.src = d.dst`), and `linear_equiv_of_module_hom_modules_linearly_equivalent` (a module homomorphism with a two-sided inverse of its underlying function witnesses `modules_linearly_equivalent(f.src, f.dst)`).
- `src/module_iso.ac` adds bundled-`LinearEquiv` inverse-map and bijection lemmas: `linear_equiv_inv_fn_is_linear_equiv` (the inverse function is a predicate-level linear equivalence), `linear_equiv_to_fn_is_injective`/`linear_equiv_to_fn_is_surjective` (the forward function is a bijection), the inverse-map value lemmas `linear_equiv_inv_map_zero/add/smul/neg/sub`, and the biconditionals `linear_equiv_to_fn_apply_eq_iff` (`e.to_fn(x) = y iff x = e.inv_fn(y)`) and `linear_equiv_map_eq_zero_iff_eq_zero` (`e.to_fn(x) = N.0 iff x = M.0`).
- `src/module_iso.ac` adds further generic bundled-`LinearEquiv` lemmas: `linear_equiv_inv_fn_is_injective`/`linear_equiv_inv_fn_is_surjective`/`linear_equiv_inv_fn_is_bijection` and `linear_equiv_to_fn_is_bijection` (the forward/inverse functions are bijections), and the disequality biconditionals `linear_equiv_to_fn_ne_iff`/`linear_equiv_inv_fn_ne_iff` (the forward/inverse function disagrees on two points iff the points differ) together with `linear_equiv_map_ne_zero_iff`/`linear_equiv_inv_map_ne_zero_iff` (the forward/inverse function maps to a nonzero value iff the input is nonzero).
- `src/module_iso.ac` adds bundled-`LinearEquiv` apply-disequality biconditionals: `linear_equiv_to_fn_apply_ne_iff` (`e.to_fn(x) != y iff x != e.inv_fn(y)`) and `linear_equiv_inv_apply_ne_iff` (`e.inv_fn(y) != x iff y != e.to_fn(x)`), the disequality companions of `linear_equiv_to_fn_apply_eq_iff`/`linear_equiv_inv_apply_eq_iff`.
- `src/module_iso.ac` adds bundled-`LinearEquiv` compose-with-symm groupoid lemmas: `linear_equiv_compose_symm_left_some`/`linear_equiv_compose_symm_right_some` (composing an equivalence with its inverse on either side is defined), with endpoint projections `linear_equiv_compose_symm_left_src`/`..._left_dst`/`..._right_src`/`..._right_dst` confirming the resulting equivalence's source and destination both equal the appropriate side of the original equivalence.
- `src/module_iso.ac` adds the bundled `LinearEquiv[R, M, N]` structure (fields `src`, `dst`, `to_fn`, `inv_fn`; forall-only constraint `is_linear_map(src, dst, to_fn) and is_two_sided_inverse_fn(to_fn, inv_fn)`), with generic projections `linear_equiv_to_fn_is_linear_map`, `linear_equiv_to_inv_two_sided`, `linear_equiv_left_inverse`/`linear_equiv_right_inverse` (pointwise), `linear_equiv_to_fn_is_linear_equiv`, `linear_equiv_inv_fn_is_linear_map`, `linear_equiv_is_pair`, `linear_equiv_modules_linearly_equivalent` (a bundled equivalence witnesses `modules_linearly_equivalent`), value lemmas `linear_equiv_map_zero/add/smul/neg/sub`, extensionality `linear_equiv_ext`, and the groupoid constructors `linear_equiv_identity`/`linear_equiv_symm`/`linear_equiv_compose` (the latter `Option`-valued, with existence `linear_equiv_compose_some`) each with `.src`/`.dst` endpoint projections. The forall-only constraint avoids the existential-unfold field-extraction timeout that previously blocked bundling. `src/functions.ac` adds the supporting pointwise inverse-extraction lemmas `left_inverse_fn_apply`, `right_inverse_fn_apply`, `two_sided_inverse_fn_left_apply`, `two_sided_inverse_fn_right_apply`.
