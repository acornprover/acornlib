# Indexed Set Families

Goal: support families of sets and their standard constructions without repeated local definitions.

- [ ] Audit the remaining `abs_conv.ac` Nat-tail set helpers (`nat_from`, `set_lower_bound`, decreasing empty-intersection bounds) and move any reusable set-family facts into `set.ac`
