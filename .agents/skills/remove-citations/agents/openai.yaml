interface:
  display_name: "Remove Citations"
  short_description: "Replace explicit theorem citations with local proof steps"
  default_prompt: "Remove explicit theorem citations from Acorn proofs by replacing each citation with local proof steps, verifying after each attempted change, and keeping the repository in a passing state."
