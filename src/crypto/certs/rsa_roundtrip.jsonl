{"goal":"d * e = e * d","p":[{"c":"function(x0: Nat, x1: Nat) { x0 * x1 = x1 * x0 }(d, e)"}]}
{"goal":"lib(nat).nat_mod(d * e)((p - 1) * (q - 1)) = lib(nat).nat_mod(e * d)((p - 1) * (q - 1))","p":[]}
{"goal":"is_rsa_key(p, q, d, e)","p":[]}
{"goal":"rsa_key_symmetric","p":[]}
{"goal":"rsa_decrypt(rsa_encrypt(m, e, p * q), d, p * q) = lib(nat).nat_mod(lib(nat).nat_mod(m.pow(e))(p * q).pow(d))(p * q)","p":[{"c":"function(x0: Nat, x1: Nat, x2: Nat) { lib(nat).nat_mod(x0.pow(x1), x2) = rsa_decrypt(x0, x1, x2) }(rsa_encrypt(m, e, p * q), d, p * q)"},{"r":"ext","c":"(forall(x0: Nat, x1: Nat) { lib(nat).nat_mod(x0.pow(x1)) = rsa_decrypt(x0, x1) })","f":[0]},{"c":"function(x0: Nat, x1: Nat, x2: Nat) { lib(nat).nat_mod(x0.pow(x1), x2) = rsa_encrypt(x0, x1, x2) }(m, e, p * q)"},{"r":"ext","c":"(forall(x0: Nat, x1: Nat) { lib(nat).nat_mod(x0.pow(x1)) = rsa_encrypt(x0, x1) })","f":[2]}]}
{"goal":"lib(nat).nat_mod(lib(nat).nat_mod(m.pow(e))(p * q).pow(d))(p * q) = lib(nat).nat_mod(m.pow(e * d))(p * q)","p":[]}
{"goal":"m.pow(e * d).congr_mod(m, p * q)","p":[]}
{"goal":"lib(nat).nat_mod(m.pow(e * d))(p * q) = lib(nat).nat_mod(m)(p * q)","p":[{"c":"function(x0: Nat, x1: Nat, x2: Nat) { lib(nat).nat_mod(x0, x1) = lib(nat).nat_mod(x2, x1) = x0.congr_mod(x2, x1) }(m.pow(e * d), p * q, m)"},{"r":"br","c":"not m.pow(e * d).congr_mod(m, p * q) or lib(nat).nat_mod(m.pow(e * d), p * q) = lib(nat).nat_mod(m, p * q)","f":[0]},{"r":"br","c":"lib(nat).nat_mod(m.pow(e * d), p * q) != lib(nat).nat_mod(m, p * q) or m.pow(e * d).congr_mod(m, p * q)","f":[0]},{"r":"inj","c":"m.pow(e * d) != m or m.pow(e * d).congr_mod(m, p * q)","f":[2]}]}
{"goal":"rsa_decrypt(rsa_encrypt(m, e, p * q), d, p * q) = lib(nat).nat_mod(m)(p * q)","p":[]}
{"goal":"rsa_decrypt_encrypt_mod","p":[]}
{"goal":"is_rsa_key(p, q, d, e)","p":[]}
{"goal":"rsa_decrypt(rsa_encrypt(m, d, p * q), e, p * q) = m","p":[]}
{"goal":"rsa_decrypt(rsa_encrypt(m, d, p * q), e, p * q) = lib(nat).nat_mod(lib(nat).nat_mod(m.pow(d))(p * q).pow(e))(p * q)","p":[{"c":"function(x0: Nat, x1: Nat, x2: Nat) { lib(nat).nat_mod(x0.pow(x1), x2) = rsa_decrypt(x0, x1, x2) }(rsa_encrypt(m, d, p * q), e, p * q)"},{"r":"ext","c":"(forall(x0: Nat, x1: Nat) { lib(nat).nat_mod(x0.pow(x1)) = rsa_decrypt(x0, x1) })","f":[0]},{"c":"function(x0: Nat, x1: Nat, x2: Nat) { lib(nat).nat_mod(x0.pow(x1), x2) = rsa_encrypt(x0, x1, x2) }(m, d, p * q)"},{"r":"ext","c":"(forall(x0: Nat, x1: Nat) { lib(nat).nat_mod(x0.pow(x1)) = rsa_encrypt(x0, x1) })","f":[2]}]}
{"goal":"rsa_encrypt(rsa_decrypt(m, d, p * q), e, p * q) = lib(nat).nat_mod(lib(nat).nat_mod(m.pow(d))(p * q).pow(e))(p * q)","p":[{"c":"function(x0: Nat, x1: Nat, x2: Nat) { lib(nat).nat_mod(x0.pow(x1), x2) = rsa_decrypt(x0, x1, x2) }(m, d, p * q)"},{"r":"ext","c":"(forall(x0: Nat, x1: Nat) { lib(nat).nat_mod(x0.pow(x1)) = rsa_decrypt(x0, x1) })","f":[0]},{"c":"function(x0: Nat, x1: Nat, x2: Nat) { lib(nat).nat_mod(x0.pow(x1), x2) = rsa_encrypt(x0, x1, x2) }(lib(nat).nat_mod(m.pow(d), p * q), e, p * q)"},{"r":"ext","c":"(forall(x0: Nat, x1: Nat) { lib(nat).nat_mod(x0.pow(x1)) = rsa_encrypt(x0, x1) })","f":[2]}]}
{"goal":"rsa_encrypt(rsa_decrypt(m, d, p * q), e, p * q) = m","p":[]}
{"goal":"rsa_encrypt_decrypt_bounded","p":[]}
{"goal":"is_rsa_key(p, q, d, e)","p":[]}
{"goal":"rsa_decrypt(rsa_encrypt(m, d, p * q), e, p * q) = lib(nat).nat_mod(m)(p * q)","p":[]}
{"goal":"rsa_decrypt(rsa_encrypt(m, d, p * q), e, p * q) = lib(nat).nat_mod(lib(nat).nat_mod(m.pow(d))(p * q).pow(e))(p * q)","p":[{"c":"function(x0: Nat, x1: Nat, x2: Nat) { lib(nat).nat_mod(x0.pow(x1), x2) = rsa_decrypt(x0, x1, x2) }(rsa_encrypt(m, d, p * q), e, p * q)"},{"r":"ext","c":"(forall(x0: Nat, x1: Nat) { lib(nat).nat_mod(x0.pow(x1)) = rsa_decrypt(x0, x1) })","f":[0]},{"c":"function(x0: Nat, x1: Nat, x2: Nat) { lib(nat).nat_mod(x0.pow(x1), x2) = rsa_encrypt(x0, x1, x2) }(m, d, p * q)"},{"r":"ext","c":"(forall(x0: Nat, x1: Nat) { lib(nat).nat_mod(x0.pow(x1)) = rsa_encrypt(x0, x1) })","f":[2]}]}
{"goal":"rsa_encrypt(rsa_decrypt(m, d, p * q), e, p * q) = lib(nat).nat_mod(lib(nat).nat_mod(m.pow(d))(p * q).pow(e))(p * q)","p":[{"c":"function(x0: Nat, x1: Nat, x2: Nat) { lib(nat).nat_mod(x0.pow(x1), x2) = rsa_decrypt(x0, x1, x2) }(m, d, p * q)"},{"r":"ext","c":"(forall(x0: Nat, x1: Nat) { lib(nat).nat_mod(x0.pow(x1)) = rsa_decrypt(x0, x1) })","f":[0]},{"c":"function(x0: Nat, x1: Nat, x2: Nat) { lib(nat).nat_mod(x0.pow(x1), x2) = rsa_encrypt(x0, x1, x2) }(lib(nat).nat_mod(m.pow(d), p * q), e, p * q)"},{"r":"ext","c":"(forall(x0: Nat, x1: Nat) { lib(nat).nat_mod(x0.pow(x1)) = rsa_encrypt(x0, x1) })","f":[2]}]}
{"goal":"rsa_encrypt(rsa_decrypt(m, d, p * q), e, p * q) = lib(nat).nat_mod(m)(p * q)","p":[]}
{"goal":"rsa_encrypt_decrypt_mod","p":[]}
{"goal":"rsa_roundtrip_mod_pair","p":[{"c":"function(x0: Nat, x1: Nat, x2: Nat, x3: Nat, x4: Nat) { not is_rsa_key(x0, x1, x2, x3) or rsa_decrypt(rsa_encrypt(x4, x2, x0 * x1), x3, x0 * x1) = lib(nat).nat_mod(x4, x0 * x1) }(p, q, e, d, m)"}]}
