{"goal":"d * e = e * d","proof":["function(x0: Nat, x1: Nat) { x0 * x1 = x1 * x0 }(d, e)"]}
{"goal":"lib(nat).nat_mod(d * e)((p - 1) * (q - 1)) = lib(nat).nat_mod(e * d)((p - 1) * (q - 1))","proof":[]}
{"goal":"is_rsa_key(p, q, d, e)","proof":[]}
{"goal":"rsa_key_symmetric","proof":[]}
{"goal":"rsa_decrypt(rsa_encrypt(m, e, p * q), d, p * q) = lib(nat).nat_mod(lib(nat).nat_mod(m.pow(e))(p * q).pow(d))(p * q)","proof":["function(x0: Nat, x1: Nat, x2: Nat) { lib(nat).nat_mod(x0.pow(x1), x2) = rsa_decrypt(x0, x1, x2) }(rsa_encrypt(m, e, p * q), d, p * q)","function(x0: Nat, x1: Nat, x2: Nat) { lib(nat).nat_mod(x0.pow(x1), x2) = rsa_encrypt(x0, x1, x2) }(m, e, p * q)"]}
{"goal":"lib(nat).nat_mod(lib(nat).nat_mod(m.pow(e))(p * q).pow(d))(p * q) = lib(nat).nat_mod(m.pow(e * d))(p * q)","proof":[]}
{"goal":"m.pow(e * d).congr_mod(m, p * q)","proof":[]}
{"goal":"lib(nat).nat_mod(m.pow(e * d))(p * q) = lib(nat).nat_mod(m)(p * q)","proof":["function(x0: Nat, x1: Nat, x2: Nat) { lib(nat).nat_mod(x0, x1) = lib(nat).nat_mod(x2, x1) = x0.congr_mod(x2, x1) }(m.pow(e * d), p * q, m)"]}
{"goal":"rsa_decrypt(rsa_encrypt(m, e, p * q), d, p * q) = lib(nat).nat_mod(m)(p * q)","proof":[]}
{"goal":"rsa_decrypt_encrypt_mod","proof":[]}
{"goal":"is_rsa_key(p, q, d, e)","proof":[]}
{"goal":"rsa_decrypt(rsa_encrypt(m, d, p * q), e, p * q) = m","proof":[]}
{"goal":"rsa_decrypt(rsa_encrypt(m, d, p * q), e, p * q) = lib(nat).nat_mod(lib(nat).nat_mod(m.pow(d))(p * q).pow(e))(p * q)","proof":["function(x0: Nat, x1: Nat, x2: Nat) { lib(nat).nat_mod(x0.pow(x1), x2) = rsa_decrypt(x0, x1, x2) }(rsa_encrypt(m, d, p * q), e, p * q)","function(x0: Nat, x1: Nat, x2: Nat) { lib(nat).nat_mod(x0.pow(x1), x2) = rsa_encrypt(x0, x1, x2) }(m, d, p * q)"]}
{"goal":"rsa_encrypt(rsa_decrypt(m, d, p * q), e, p * q) = lib(nat).nat_mod(lib(nat).nat_mod(m.pow(d))(p * q).pow(e))(p * q)","proof":["function(x0: Nat, x1: Nat, x2: Nat) { lib(nat).nat_mod(x0.pow(x1), x2) = rsa_decrypt(x0, x1, x2) }(m, d, p * q)","function(x0: Nat, x1: Nat, x2: Nat) { lib(nat).nat_mod(x0.pow(x1), x2) = rsa_encrypt(x0, x1, x2) }(lib(nat).nat_mod(m.pow(d), p * q), e, p * q)"]}
{"goal":"rsa_encrypt(rsa_decrypt(m, d, p * q), e, p * q) = m","proof":[]}
{"goal":"rsa_encrypt_decrypt_bounded","proof":[]}
{"goal":"is_rsa_key(p, q, d, e)","proof":[]}
{"goal":"rsa_decrypt(rsa_encrypt(m, d, p * q), e, p * q) = lib(nat).nat_mod(m)(p * q)","proof":[]}
{"goal":"rsa_decrypt(rsa_encrypt(m, d, p * q), e, p * q) = lib(nat).nat_mod(lib(nat).nat_mod(m.pow(d))(p * q).pow(e))(p * q)","proof":["function(x0: Nat, x1: Nat, x2: Nat) { lib(nat).nat_mod(x0.pow(x1), x2) = rsa_decrypt(x0, x1, x2) }(rsa_encrypt(m, d, p * q), e, p * q)","function(x0: Nat, x1: Nat, x2: Nat) { lib(nat).nat_mod(x0.pow(x1), x2) = rsa_encrypt(x0, x1, x2) }(m, d, p * q)"]}
{"goal":"rsa_encrypt(rsa_decrypt(m, d, p * q), e, p * q) = lib(nat).nat_mod(lib(nat).nat_mod(m.pow(d))(p * q).pow(e))(p * q)","proof":["function(x0: Nat, x1: Nat, x2: Nat) { lib(nat).nat_mod(x0.pow(x1), x2) = rsa_decrypt(x0, x1, x2) }(m, d, p * q)","function(x0: Nat, x1: Nat, x2: Nat) { lib(nat).nat_mod(x0.pow(x1), x2) = rsa_encrypt(x0, x1, x2) }(lib(nat).nat_mod(m.pow(d), p * q), e, p * q)"]}
{"goal":"rsa_encrypt(rsa_decrypt(m, d, p * q), e, p * q) = lib(nat).nat_mod(m)(p * q)","proof":[]}
{"goal":"rsa_encrypt_decrypt_mod","proof":[]}
