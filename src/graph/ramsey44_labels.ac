from nat import Nat, lt_and_lte

numerals Nat

/// The labels 0 to 8 are ordered.
theorem eighteen_labels_lt_ladder_a {
    Nat.0 < Nat.1 and Nat.1 < Nat.2 and Nat.2 < Nat.3 and Nat.3 < Nat.4 and Nat.4 < Nat.5 and Nat.5 < Nat.6 and Nat.6 < Nat.7 and Nat.7 < Nat.8 and Nat.8 < Nat.9
} by {
    Nat.0.suc = Nat.1
    Nat.0 < Nat.0.suc
    Nat.0 < Nat.1
    Nat.1.suc = Nat.2
    Nat.1 < Nat.1.suc
    Nat.1 < Nat.2
    Nat.2.suc = Nat.3
    Nat.2 < Nat.2.suc
    Nat.2 < Nat.3
    Nat.3.suc = Nat.4
    Nat.3 < Nat.3.suc
    Nat.3 < Nat.4
    Nat.4.suc = Nat.5
    Nat.4 < Nat.4.suc
    Nat.4 < Nat.5
    Nat.5.suc = Nat.6
    Nat.5 < Nat.5.suc
    Nat.5 < Nat.6
    Nat.6.suc = Nat.7
    Nat.6 < Nat.6.suc
    Nat.6 < Nat.7
    Nat.7.suc = Nat.8
    Nat.7 < Nat.7.suc
    Nat.7 < Nat.8
    Nat.8.suc = Nat.9
    Nat.8 < Nat.8.suc
    Nat.8 < Nat.9
}

/// The labels 9 to 17 are ordered.
theorem eighteen_labels_lt_ladder_b {
    Nat.9 < Nat.10 and Nat.10 < Nat.11 and Nat.11 < Nat.12 and Nat.12 < Nat.13 and Nat.13 < Nat.14 and Nat.14 < Nat.15 and Nat.15 < Nat.16 and Nat.16 < Nat.17 and Nat.17 < Nat.18
} by {
    Nat.9.suc = Nat.10
    Nat.9 < Nat.9.suc
    Nat.9 < Nat.10
    Nat.10.suc = Nat.11
    Nat.10 < Nat.10.suc
    Nat.10 < Nat.11
    Nat.11.suc = Nat.12
    Nat.11 < Nat.11.suc
    Nat.11 < Nat.12
    Nat.12.suc = Nat.13
    Nat.12 < Nat.12.suc
    Nat.12 < Nat.13
    Nat.13.suc = Nat.14
    Nat.13 < Nat.13.suc
    Nat.13 < Nat.14
    Nat.14.suc = Nat.15
    Nat.14 < Nat.14.suc
    Nat.14 < Nat.15
    Nat.15.suc = Nat.16
    Nat.15 < Nat.15.suc
    Nat.15 < Nat.16
    Nat.16.suc = Nat.17
    Nat.16 < Nat.16.suc
    Nat.16 < Nat.17
    Nat.17.suc = Nat.18
    Nat.17 < Nat.17.suc
    Nat.17 < Nat.18
}

/// The labels of the eighteen vertices are ordered.
///
/// Comparisons between numerals are not free, so the ladder is stated once and read off.
theorem eighteen_labels_lt_ladder {
    Nat.0 < Nat.1 and Nat.1 < Nat.2 and Nat.2 < Nat.3 and Nat.3 < Nat.4 and Nat.4 < Nat.5 and Nat.5 < Nat.6 and Nat.6 < Nat.7 and Nat.7 < Nat.8 and Nat.8 < Nat.9 and Nat.9 < Nat.10 and Nat.10 < Nat.11 and Nat.11 < Nat.12 and Nat.12 < Nat.13 and Nat.13 < Nat.14 and Nat.14 < Nat.15 and Nat.15 < Nat.16 and Nat.16 < Nat.17
} by {
    eighteen_labels_lt_ladder_a
    Nat.0 < Nat.1 and Nat.1 < Nat.2 and Nat.2 < Nat.3 and Nat.3 < Nat.4 and Nat.4 < Nat.5 and Nat.5 < Nat.6 and Nat.6 < Nat.7 and Nat.7 < Nat.8 and Nat.8 < Nat.9
    eighteen_labels_lt_ladder_b
    Nat.9 < Nat.10 and Nat.10 < Nat.11 and Nat.11 < Nat.12 and Nat.12 < Nat.13 and Nat.13 < Nat.14 and Nat.14 < Nat.15 and Nat.15 < Nat.16 and Nat.16 < Nat.17
}

/// The labels 0 to 8 are below eighteen.
theorem eighteen_labels_lt_bound_a {
    Nat.0 < Nat.18 and Nat.1 < Nat.18 and Nat.2 < Nat.18 and Nat.3 < Nat.18 and Nat.4 < Nat.18 and Nat.5 < Nat.18 and Nat.6 < Nat.18 and Nat.7 < Nat.18 and Nat.8 < Nat.18
} by {
    eighteen_labels_lt_ladder_a
    Nat.0 < Nat.1 and Nat.1 < Nat.2 and Nat.2 < Nat.3 and Nat.3 < Nat.4 and Nat.4 < Nat.5 and Nat.5 < Nat.6 and Nat.6 < Nat.7 and Nat.7 < Nat.8 and Nat.8 < Nat.9
    eighteen_labels_lt_ladder_b
    Nat.9 < Nat.10 and Nat.10 < Nat.11 and Nat.11 < Nat.12 and Nat.12 < Nat.13 and Nat.13 < Nat.14 and Nat.14 < Nat.15 and Nat.15 < Nat.16 and Nat.16 < Nat.17
    Nat.9 <= Nat.18
    lt_and_lte(Nat.8, Nat.9, Nat.18)
    Nat.8 < Nat.18
    Nat.8 <= Nat.18
    lt_and_lte(Nat.7, Nat.8, Nat.18)
    Nat.7 < Nat.18
    Nat.7 <= Nat.18
    lt_and_lte(Nat.6, Nat.7, Nat.18)
    Nat.6 < Nat.18
    Nat.6 <= Nat.18
    lt_and_lte(Nat.5, Nat.6, Nat.18)
    Nat.5 < Nat.18
    Nat.5 <= Nat.18
    lt_and_lte(Nat.4, Nat.5, Nat.18)
    Nat.4 < Nat.18
    Nat.4 <= Nat.18
    lt_and_lte(Nat.3, Nat.4, Nat.18)
    Nat.3 < Nat.18
    Nat.3 <= Nat.18
    lt_and_lte(Nat.2, Nat.3, Nat.18)
    Nat.2 < Nat.18
    Nat.2 <= Nat.18
    lt_and_lte(Nat.1, Nat.2, Nat.18)
    Nat.1 < Nat.18
    Nat.1 <= Nat.18
    lt_and_lte(Nat.0, Nat.1, Nat.18)
    Nat.0 < Nat.18
}

/// The labels 9 to 17 are below eighteen.
theorem eighteen_labels_lt_bound_b {
    Nat.9 < Nat.18 and Nat.10 < Nat.18 and Nat.11 < Nat.18 and Nat.12 < Nat.18 and Nat.13 < Nat.18 and Nat.14 < Nat.18 and Nat.15 < Nat.18 and Nat.16 < Nat.18 and Nat.17 < Nat.18
} by {
    eighteen_labels_lt_ladder_a
    Nat.0 < Nat.1 and Nat.1 < Nat.2 and Nat.2 < Nat.3 and Nat.3 < Nat.4 and Nat.4 < Nat.5 and Nat.5 < Nat.6 and Nat.6 < Nat.7 and Nat.7 < Nat.8 and Nat.8 < Nat.9
    eighteen_labels_lt_ladder_b
    Nat.9 < Nat.10 and Nat.10 < Nat.11 and Nat.11 < Nat.12 and Nat.12 < Nat.13 and Nat.13 < Nat.14 and Nat.14 < Nat.15 and Nat.15 < Nat.16 and Nat.16 < Nat.17
    Nat.17 < Nat.18
    Nat.17 <= Nat.18
    lt_and_lte(Nat.16, Nat.17, Nat.18)
    Nat.16 < Nat.18
    Nat.16 <= Nat.18
    lt_and_lte(Nat.15, Nat.16, Nat.18)
    Nat.15 < Nat.18
    Nat.15 <= Nat.18
    lt_and_lte(Nat.14, Nat.15, Nat.18)
    Nat.14 < Nat.18
    Nat.14 <= Nat.18
    lt_and_lte(Nat.13, Nat.14, Nat.18)
    Nat.13 < Nat.18
    Nat.13 <= Nat.18
    lt_and_lte(Nat.12, Nat.13, Nat.18)
    Nat.12 < Nat.18
    Nat.12 <= Nat.18
    lt_and_lte(Nat.11, Nat.12, Nat.18)
    Nat.11 < Nat.18
    Nat.11 <= Nat.18
    lt_and_lte(Nat.10, Nat.11, Nat.18)
    Nat.10 < Nat.18
    Nat.10 <= Nat.18
    lt_and_lte(Nat.9, Nat.10, Nat.18)
    Nat.9 < Nat.18
}

/// Every label is below eighteen.
///
/// The flat conjunction of all eighteen bound facts is stated once and read off.
theorem eighteen_labels_lt_bound {
    Nat.0 < Nat.18 and Nat.1 < Nat.18 and Nat.2 < Nat.18 and Nat.3 < Nat.18 and Nat.4 < Nat.18 and Nat.5 < Nat.18 and Nat.6 < Nat.18 and Nat.7 < Nat.18 and Nat.8 < Nat.18 and Nat.9 < Nat.18 and Nat.10 < Nat.18 and Nat.11 < Nat.18 and Nat.12 < Nat.18 and Nat.13 < Nat.18 and Nat.14 < Nat.18 and Nat.15 < Nat.18 and Nat.16 < Nat.18 and Nat.17 < Nat.18
} by {
    eighteen_labels_lt_bound_a
    Nat.0 < Nat.18 and Nat.1 < Nat.18 and Nat.2 < Nat.18 and Nat.3 < Nat.18 and Nat.4 < Nat.18 and Nat.5 < Nat.18 and Nat.6 < Nat.18 and Nat.7 < Nat.18 and Nat.8 < Nat.18
    eighteen_labels_lt_bound_b
    Nat.9 < Nat.18 and Nat.10 < Nat.18 and Nat.11 < Nat.18 and Nat.12 < Nat.18 and Nat.13 < Nat.18 and Nat.14 < Nat.18 and Nat.15 < Nat.18 and Nat.16 < Nat.18 and Nat.17 < Nat.18
}

/// The label 0 is distinct from every label above it.
theorem eighteen_labels_distinct_row0 {
    Nat.0 != Nat.1 and Nat.0 != Nat.2 and Nat.0 != Nat.3 and Nat.0 != Nat.4 and Nat.0 != Nat.5 and Nat.0 != Nat.6 and Nat.0 != Nat.7 and Nat.0 != Nat.8 and Nat.0 != Nat.9 and Nat.0 != Nat.10 and Nat.0 != Nat.11 and Nat.0 != Nat.12 and Nat.0 != Nat.13 and Nat.0 != Nat.14 and Nat.0 != Nat.15 and Nat.0 != Nat.16 and Nat.0 != Nat.17
} by {
    eighteen_labels_lt_ladder
    (Nat.0 < Nat.1 and Nat.1 < Nat.2 and Nat.2 < Nat.3 and Nat.3 < Nat.4 and Nat.4 < Nat.5 and Nat.5 < Nat.6 and Nat.6 < Nat.7 and Nat.7 < Nat.8 and Nat.8 < Nat.9 and Nat.9 < Nat.10 and Nat.10 < Nat.11 and Nat.11 < Nat.12 and Nat.12 < Nat.13 and Nat.13 < Nat.14 and Nat.14 < Nat.15 and Nat.15 < Nat.16 and Nat.16 < Nat.17)
    Nat.0 <= Nat.1
    lt_and_lte(Nat.0, Nat.0, Nat.1)
    Nat.0 < Nat.1
    Nat.0 != Nat.1
    Nat.1 <= Nat.2
    lt_and_lte(Nat.0, Nat.1, Nat.2)
    Nat.0 < Nat.2
    Nat.0 != Nat.2
    Nat.2 <= Nat.3
    lt_and_lte(Nat.0, Nat.2, Nat.3)
    Nat.0 < Nat.3
    Nat.0 != Nat.3
    Nat.3 <= Nat.4
    lt_and_lte(Nat.0, Nat.3, Nat.4)
    Nat.0 < Nat.4
    Nat.0 != Nat.4
    Nat.4 <= Nat.5
    lt_and_lte(Nat.0, Nat.4, Nat.5)
    Nat.0 < Nat.5
    Nat.0 != Nat.5
    Nat.5 <= Nat.6
    lt_and_lte(Nat.0, Nat.5, Nat.6)
    Nat.0 < Nat.6
    Nat.0 != Nat.6
    Nat.6 <= Nat.7
    lt_and_lte(Nat.0, Nat.6, Nat.7)
    Nat.0 < Nat.7
    Nat.0 != Nat.7
    Nat.7 <= Nat.8
    lt_and_lte(Nat.0, Nat.7, Nat.8)
    Nat.0 < Nat.8
    Nat.0 != Nat.8
    Nat.8 <= Nat.9
    lt_and_lte(Nat.0, Nat.8, Nat.9)
    Nat.0 < Nat.9
    Nat.0 != Nat.9
    Nat.9 <= Nat.10
    lt_and_lte(Nat.0, Nat.9, Nat.10)
    Nat.0 < Nat.10
    Nat.0 != Nat.10
    Nat.10 <= Nat.11
    lt_and_lte(Nat.0, Nat.10, Nat.11)
    Nat.0 < Nat.11
    Nat.0 != Nat.11
    Nat.11 <= Nat.12
    lt_and_lte(Nat.0, Nat.11, Nat.12)
    Nat.0 < Nat.12
    Nat.0 != Nat.12
    Nat.12 <= Nat.13
    lt_and_lte(Nat.0, Nat.12, Nat.13)
    Nat.0 < Nat.13
    Nat.0 != Nat.13
    Nat.13 <= Nat.14
    lt_and_lte(Nat.0, Nat.13, Nat.14)
    Nat.0 < Nat.14
    Nat.0 != Nat.14
    Nat.14 <= Nat.15
    lt_and_lte(Nat.0, Nat.14, Nat.15)
    Nat.0 < Nat.15
    Nat.0 != Nat.15
    Nat.15 <= Nat.16
    lt_and_lte(Nat.0, Nat.15, Nat.16)
    Nat.0 < Nat.16
    Nat.0 != Nat.16
    Nat.16 <= Nat.17
    lt_and_lte(Nat.0, Nat.16, Nat.17)
    Nat.0 < Nat.17
    Nat.0 != Nat.17
}

/// The label 1 is distinct from every label above it.
theorem eighteen_labels_distinct_row1 {
    Nat.1 != Nat.2 and Nat.1 != Nat.3 and Nat.1 != Nat.4 and Nat.1 != Nat.5 and Nat.1 != Nat.6 and Nat.1 != Nat.7 and Nat.1 != Nat.8 and Nat.1 != Nat.9 and Nat.1 != Nat.10 and Nat.1 != Nat.11 and Nat.1 != Nat.12 and Nat.1 != Nat.13 and Nat.1 != Nat.14 and Nat.1 != Nat.15 and Nat.1 != Nat.16 and Nat.1 != Nat.17
} by {
    eighteen_labels_lt_ladder
    (Nat.0 < Nat.1 and Nat.1 < Nat.2 and Nat.2 < Nat.3 and Nat.3 < Nat.4 and Nat.4 < Nat.5 and Nat.5 < Nat.6 and Nat.6 < Nat.7 and Nat.7 < Nat.8 and Nat.8 < Nat.9 and Nat.9 < Nat.10 and Nat.10 < Nat.11 and Nat.11 < Nat.12 and Nat.12 < Nat.13 and Nat.13 < Nat.14 and Nat.14 < Nat.15 and Nat.15 < Nat.16 and Nat.16 < Nat.17)
    Nat.1 <= Nat.2
    lt_and_lte(Nat.1, Nat.1, Nat.2)
    Nat.1 < Nat.2
    Nat.1 != Nat.2
    Nat.2 <= Nat.3
    lt_and_lte(Nat.1, Nat.2, Nat.3)
    Nat.1 < Nat.3
    Nat.1 != Nat.3
    Nat.3 <= Nat.4
    lt_and_lte(Nat.1, Nat.3, Nat.4)
    Nat.1 < Nat.4
    Nat.1 != Nat.4
    Nat.4 <= Nat.5
    lt_and_lte(Nat.1, Nat.4, Nat.5)
    Nat.1 < Nat.5
    Nat.1 != Nat.5
    Nat.5 <= Nat.6
    lt_and_lte(Nat.1, Nat.5, Nat.6)
    Nat.1 < Nat.6
    Nat.1 != Nat.6
    Nat.6 <= Nat.7
    lt_and_lte(Nat.1, Nat.6, Nat.7)
    Nat.1 < Nat.7
    Nat.1 != Nat.7
    Nat.7 <= Nat.8
    lt_and_lte(Nat.1, Nat.7, Nat.8)
    Nat.1 < Nat.8
    Nat.1 != Nat.8
    Nat.8 <= Nat.9
    lt_and_lte(Nat.1, Nat.8, Nat.9)
    Nat.1 < Nat.9
    Nat.1 != Nat.9
    Nat.9 <= Nat.10
    lt_and_lte(Nat.1, Nat.9, Nat.10)
    Nat.1 < Nat.10
    Nat.1 != Nat.10
    Nat.10 <= Nat.11
    lt_and_lte(Nat.1, Nat.10, Nat.11)
    Nat.1 < Nat.11
    Nat.1 != Nat.11
    Nat.11 <= Nat.12
    lt_and_lte(Nat.1, Nat.11, Nat.12)
    Nat.1 < Nat.12
    Nat.1 != Nat.12
    Nat.12 <= Nat.13
    lt_and_lte(Nat.1, Nat.12, Nat.13)
    Nat.1 < Nat.13
    Nat.1 != Nat.13
    Nat.13 <= Nat.14
    lt_and_lte(Nat.1, Nat.13, Nat.14)
    Nat.1 < Nat.14
    Nat.1 != Nat.14
    Nat.14 <= Nat.15
    lt_and_lte(Nat.1, Nat.14, Nat.15)
    Nat.1 < Nat.15
    Nat.1 != Nat.15
    Nat.15 <= Nat.16
    lt_and_lte(Nat.1, Nat.15, Nat.16)
    Nat.1 < Nat.16
    Nat.1 != Nat.16
    Nat.16 <= Nat.17
    lt_and_lte(Nat.1, Nat.16, Nat.17)
    Nat.1 < Nat.17
    Nat.1 != Nat.17
}

/// The label 2 is distinct from every label above it.
theorem eighteen_labels_distinct_row2 {
    Nat.2 != Nat.3 and Nat.2 != Nat.4 and Nat.2 != Nat.5 and Nat.2 != Nat.6 and Nat.2 != Nat.7 and Nat.2 != Nat.8 and Nat.2 != Nat.9 and Nat.2 != Nat.10 and Nat.2 != Nat.11 and Nat.2 != Nat.12 and Nat.2 != Nat.13 and Nat.2 != Nat.14 and Nat.2 != Nat.15 and Nat.2 != Nat.16 and Nat.2 != Nat.17
} by {
    eighteen_labels_lt_ladder
    (Nat.0 < Nat.1 and Nat.1 < Nat.2 and Nat.2 < Nat.3 and Nat.3 < Nat.4 and Nat.4 < Nat.5 and Nat.5 < Nat.6 and Nat.6 < Nat.7 and Nat.7 < Nat.8 and Nat.8 < Nat.9 and Nat.9 < Nat.10 and Nat.10 < Nat.11 and Nat.11 < Nat.12 and Nat.12 < Nat.13 and Nat.13 < Nat.14 and Nat.14 < Nat.15 and Nat.15 < Nat.16 and Nat.16 < Nat.17)
    Nat.2 <= Nat.3
    lt_and_lte(Nat.2, Nat.2, Nat.3)
    Nat.2 < Nat.3
    Nat.2 != Nat.3
    Nat.3 <= Nat.4
    lt_and_lte(Nat.2, Nat.3, Nat.4)
    Nat.2 < Nat.4
    Nat.2 != Nat.4
    Nat.4 <= Nat.5
    lt_and_lte(Nat.2, Nat.4, Nat.5)
    Nat.2 < Nat.5
    Nat.2 != Nat.5
    Nat.5 <= Nat.6
    lt_and_lte(Nat.2, Nat.5, Nat.6)
    Nat.2 < Nat.6
    Nat.2 != Nat.6
    Nat.6 <= Nat.7
    lt_and_lte(Nat.2, Nat.6, Nat.7)
    Nat.2 < Nat.7
    Nat.2 != Nat.7
    Nat.7 <= Nat.8
    lt_and_lte(Nat.2, Nat.7, Nat.8)
    Nat.2 < Nat.8
    Nat.2 != Nat.8
    Nat.8 <= Nat.9
    lt_and_lte(Nat.2, Nat.8, Nat.9)
    Nat.2 < Nat.9
    Nat.2 != Nat.9
    Nat.9 <= Nat.10
    lt_and_lte(Nat.2, Nat.9, Nat.10)
    Nat.2 < Nat.10
    Nat.2 != Nat.10
    Nat.10 <= Nat.11
    lt_and_lte(Nat.2, Nat.10, Nat.11)
    Nat.2 < Nat.11
    Nat.2 != Nat.11
    Nat.11 <= Nat.12
    lt_and_lte(Nat.2, Nat.11, Nat.12)
    Nat.2 < Nat.12
    Nat.2 != Nat.12
    Nat.12 <= Nat.13
    lt_and_lte(Nat.2, Nat.12, Nat.13)
    Nat.2 < Nat.13
    Nat.2 != Nat.13
    Nat.13 <= Nat.14
    lt_and_lte(Nat.2, Nat.13, Nat.14)
    Nat.2 < Nat.14
    Nat.2 != Nat.14
    Nat.14 <= Nat.15
    lt_and_lte(Nat.2, Nat.14, Nat.15)
    Nat.2 < Nat.15
    Nat.2 != Nat.15
    Nat.15 <= Nat.16
    lt_and_lte(Nat.2, Nat.15, Nat.16)
    Nat.2 < Nat.16
    Nat.2 != Nat.16
    Nat.16 <= Nat.17
    lt_and_lte(Nat.2, Nat.16, Nat.17)
    Nat.2 < Nat.17
    Nat.2 != Nat.17
}

/// The label 3 is distinct from every label above it.
theorem eighteen_labels_distinct_row3 {
    Nat.3 != Nat.4 and Nat.3 != Nat.5 and Nat.3 != Nat.6 and Nat.3 != Nat.7 and Nat.3 != Nat.8 and Nat.3 != Nat.9 and Nat.3 != Nat.10 and Nat.3 != Nat.11 and Nat.3 != Nat.12 and Nat.3 != Nat.13 and Nat.3 != Nat.14 and Nat.3 != Nat.15 and Nat.3 != Nat.16 and Nat.3 != Nat.17
} by {
    eighteen_labels_lt_ladder
    (Nat.0 < Nat.1 and Nat.1 < Nat.2 and Nat.2 < Nat.3 and Nat.3 < Nat.4 and Nat.4 < Nat.5 and Nat.5 < Nat.6 and Nat.6 < Nat.7 and Nat.7 < Nat.8 and Nat.8 < Nat.9 and Nat.9 < Nat.10 and Nat.10 < Nat.11 and Nat.11 < Nat.12 and Nat.12 < Nat.13 and Nat.13 < Nat.14 and Nat.14 < Nat.15 and Nat.15 < Nat.16 and Nat.16 < Nat.17)
    Nat.3 <= Nat.4
    lt_and_lte(Nat.3, Nat.3, Nat.4)
    Nat.3 < Nat.4
    Nat.3 != Nat.4
    Nat.4 <= Nat.5
    lt_and_lte(Nat.3, Nat.4, Nat.5)
    Nat.3 < Nat.5
    Nat.3 != Nat.5
    Nat.5 <= Nat.6
    lt_and_lte(Nat.3, Nat.5, Nat.6)
    Nat.3 < Nat.6
    Nat.3 != Nat.6
    Nat.6 <= Nat.7
    lt_and_lte(Nat.3, Nat.6, Nat.7)
    Nat.3 < Nat.7
    Nat.3 != Nat.7
    Nat.7 <= Nat.8
    lt_and_lte(Nat.3, Nat.7, Nat.8)
    Nat.3 < Nat.8
    Nat.3 != Nat.8
    Nat.8 <= Nat.9
    lt_and_lte(Nat.3, Nat.8, Nat.9)
    Nat.3 < Nat.9
    Nat.3 != Nat.9
    Nat.9 <= Nat.10
    lt_and_lte(Nat.3, Nat.9, Nat.10)
    Nat.3 < Nat.10
    Nat.3 != Nat.10
    Nat.10 <= Nat.11
    lt_and_lte(Nat.3, Nat.10, Nat.11)
    Nat.3 < Nat.11
    Nat.3 != Nat.11
    Nat.11 <= Nat.12
    lt_and_lte(Nat.3, Nat.11, Nat.12)
    Nat.3 < Nat.12
    Nat.3 != Nat.12
    Nat.12 <= Nat.13
    lt_and_lte(Nat.3, Nat.12, Nat.13)
    Nat.3 < Nat.13
    Nat.3 != Nat.13
    Nat.13 <= Nat.14
    lt_and_lte(Nat.3, Nat.13, Nat.14)
    Nat.3 < Nat.14
    Nat.3 != Nat.14
    Nat.14 <= Nat.15
    lt_and_lte(Nat.3, Nat.14, Nat.15)
    Nat.3 < Nat.15
    Nat.3 != Nat.15
    Nat.15 <= Nat.16
    lt_and_lte(Nat.3, Nat.15, Nat.16)
    Nat.3 < Nat.16
    Nat.3 != Nat.16
    Nat.16 <= Nat.17
    lt_and_lte(Nat.3, Nat.16, Nat.17)
    Nat.3 < Nat.17
    Nat.3 != Nat.17
}

/// The label 4 is distinct from every label above it.
theorem eighteen_labels_distinct_row4 {
    Nat.4 != Nat.5 and Nat.4 != Nat.6 and Nat.4 != Nat.7 and Nat.4 != Nat.8 and Nat.4 != Nat.9 and Nat.4 != Nat.10 and Nat.4 != Nat.11 and Nat.4 != Nat.12 and Nat.4 != Nat.13 and Nat.4 != Nat.14 and Nat.4 != Nat.15 and Nat.4 != Nat.16 and Nat.4 != Nat.17
} by {
    eighteen_labels_lt_ladder
    (Nat.0 < Nat.1 and Nat.1 < Nat.2 and Nat.2 < Nat.3 and Nat.3 < Nat.4 and Nat.4 < Nat.5 and Nat.5 < Nat.6 and Nat.6 < Nat.7 and Nat.7 < Nat.8 and Nat.8 < Nat.9 and Nat.9 < Nat.10 and Nat.10 < Nat.11 and Nat.11 < Nat.12 and Nat.12 < Nat.13 and Nat.13 < Nat.14 and Nat.14 < Nat.15 and Nat.15 < Nat.16 and Nat.16 < Nat.17)
    Nat.4 <= Nat.5
    lt_and_lte(Nat.4, Nat.4, Nat.5)
    Nat.4 < Nat.5
    Nat.4 != Nat.5
    Nat.5 <= Nat.6
    lt_and_lte(Nat.4, Nat.5, Nat.6)
    Nat.4 < Nat.6
    Nat.4 != Nat.6
    Nat.6 <= Nat.7
    lt_and_lte(Nat.4, Nat.6, Nat.7)
    Nat.4 < Nat.7
    Nat.4 != Nat.7
    Nat.7 <= Nat.8
    lt_and_lte(Nat.4, Nat.7, Nat.8)
    Nat.4 < Nat.8
    Nat.4 != Nat.8
    Nat.8 <= Nat.9
    lt_and_lte(Nat.4, Nat.8, Nat.9)
    Nat.4 < Nat.9
    Nat.4 != Nat.9
    Nat.9 <= Nat.10
    lt_and_lte(Nat.4, Nat.9, Nat.10)
    Nat.4 < Nat.10
    Nat.4 != Nat.10
    Nat.10 <= Nat.11
    lt_and_lte(Nat.4, Nat.10, Nat.11)
    Nat.4 < Nat.11
    Nat.4 != Nat.11
    Nat.11 <= Nat.12
    lt_and_lte(Nat.4, Nat.11, Nat.12)
    Nat.4 < Nat.12
    Nat.4 != Nat.12
    Nat.12 <= Nat.13
    lt_and_lte(Nat.4, Nat.12, Nat.13)
    Nat.4 < Nat.13
    Nat.4 != Nat.13
    Nat.13 <= Nat.14
    lt_and_lte(Nat.4, Nat.13, Nat.14)
    Nat.4 < Nat.14
    Nat.4 != Nat.14
    Nat.14 <= Nat.15
    lt_and_lte(Nat.4, Nat.14, Nat.15)
    Nat.4 < Nat.15
    Nat.4 != Nat.15
    Nat.15 <= Nat.16
    lt_and_lte(Nat.4, Nat.15, Nat.16)
    Nat.4 < Nat.16
    Nat.4 != Nat.16
    Nat.16 <= Nat.17
    lt_and_lte(Nat.4, Nat.16, Nat.17)
    Nat.4 < Nat.17
    Nat.4 != Nat.17
}

/// The label 5 is distinct from every label above it.
theorem eighteen_labels_distinct_row5 {
    Nat.5 != Nat.6 and Nat.5 != Nat.7 and Nat.5 != Nat.8 and Nat.5 != Nat.9 and Nat.5 != Nat.10 and Nat.5 != Nat.11 and Nat.5 != Nat.12 and Nat.5 != Nat.13 and Nat.5 != Nat.14 and Nat.5 != Nat.15 and Nat.5 != Nat.16 and Nat.5 != Nat.17
} by {
    eighteen_labels_lt_ladder
    (Nat.0 < Nat.1 and Nat.1 < Nat.2 and Nat.2 < Nat.3 and Nat.3 < Nat.4 and Nat.4 < Nat.5 and Nat.5 < Nat.6 and Nat.6 < Nat.7 and Nat.7 < Nat.8 and Nat.8 < Nat.9 and Nat.9 < Nat.10 and Nat.10 < Nat.11 and Nat.11 < Nat.12 and Nat.12 < Nat.13 and Nat.13 < Nat.14 and Nat.14 < Nat.15 and Nat.15 < Nat.16 and Nat.16 < Nat.17)
    Nat.5 <= Nat.6
    lt_and_lte(Nat.5, Nat.5, Nat.6)
    Nat.5 < Nat.6
    Nat.5 != Nat.6
    Nat.6 <= Nat.7
    lt_and_lte(Nat.5, Nat.6, Nat.7)
    Nat.5 < Nat.7
    Nat.5 != Nat.7
    Nat.7 <= Nat.8
    lt_and_lte(Nat.5, Nat.7, Nat.8)
    Nat.5 < Nat.8
    Nat.5 != Nat.8
    Nat.8 <= Nat.9
    lt_and_lte(Nat.5, Nat.8, Nat.9)
    Nat.5 < Nat.9
    Nat.5 != Nat.9
    Nat.9 <= Nat.10
    lt_and_lte(Nat.5, Nat.9, Nat.10)
    Nat.5 < Nat.10
    Nat.5 != Nat.10
    Nat.10 <= Nat.11
    lt_and_lte(Nat.5, Nat.10, Nat.11)
    Nat.5 < Nat.11
    Nat.5 != Nat.11
    Nat.11 <= Nat.12
    lt_and_lte(Nat.5, Nat.11, Nat.12)
    Nat.5 < Nat.12
    Nat.5 != Nat.12
    Nat.12 <= Nat.13
    lt_and_lte(Nat.5, Nat.12, Nat.13)
    Nat.5 < Nat.13
    Nat.5 != Nat.13
    Nat.13 <= Nat.14
    lt_and_lte(Nat.5, Nat.13, Nat.14)
    Nat.5 < Nat.14
    Nat.5 != Nat.14
    Nat.14 <= Nat.15
    lt_and_lte(Nat.5, Nat.14, Nat.15)
    Nat.5 < Nat.15
    Nat.5 != Nat.15
    Nat.15 <= Nat.16
    lt_and_lte(Nat.5, Nat.15, Nat.16)
    Nat.5 < Nat.16
    Nat.5 != Nat.16
    Nat.16 <= Nat.17
    lt_and_lte(Nat.5, Nat.16, Nat.17)
    Nat.5 < Nat.17
    Nat.5 != Nat.17
}

/// The label 6 is distinct from every label above it.
theorem eighteen_labels_distinct_row6 {
    Nat.6 != Nat.7 and Nat.6 != Nat.8 and Nat.6 != Nat.9 and Nat.6 != Nat.10 and Nat.6 != Nat.11 and Nat.6 != Nat.12 and Nat.6 != Nat.13 and Nat.6 != Nat.14 and Nat.6 != Nat.15 and Nat.6 != Nat.16 and Nat.6 != Nat.17
} by {
    eighteen_labels_lt_ladder
    (Nat.0 < Nat.1 and Nat.1 < Nat.2 and Nat.2 < Nat.3 and Nat.3 < Nat.4 and Nat.4 < Nat.5 and Nat.5 < Nat.6 and Nat.6 < Nat.7 and Nat.7 < Nat.8 and Nat.8 < Nat.9 and Nat.9 < Nat.10 and Nat.10 < Nat.11 and Nat.11 < Nat.12 and Nat.12 < Nat.13 and Nat.13 < Nat.14 and Nat.14 < Nat.15 and Nat.15 < Nat.16 and Nat.16 < Nat.17)
    Nat.6 <= Nat.7
    lt_and_lte(Nat.6, Nat.6, Nat.7)
    Nat.6 < Nat.7
    Nat.6 != Nat.7
    Nat.7 <= Nat.8
    lt_and_lte(Nat.6, Nat.7, Nat.8)
    Nat.6 < Nat.8
    Nat.6 != Nat.8
    Nat.8 <= Nat.9
    lt_and_lte(Nat.6, Nat.8, Nat.9)
    Nat.6 < Nat.9
    Nat.6 != Nat.9
    Nat.9 <= Nat.10
    lt_and_lte(Nat.6, Nat.9, Nat.10)
    Nat.6 < Nat.10
    Nat.6 != Nat.10
    Nat.10 <= Nat.11
    lt_and_lte(Nat.6, Nat.10, Nat.11)
    Nat.6 < Nat.11
    Nat.6 != Nat.11
    Nat.11 <= Nat.12
    lt_and_lte(Nat.6, Nat.11, Nat.12)
    Nat.6 < Nat.12
    Nat.6 != Nat.12
    Nat.12 <= Nat.13
    lt_and_lte(Nat.6, Nat.12, Nat.13)
    Nat.6 < Nat.13
    Nat.6 != Nat.13
    Nat.13 <= Nat.14
    lt_and_lte(Nat.6, Nat.13, Nat.14)
    Nat.6 < Nat.14
    Nat.6 != Nat.14
    Nat.14 <= Nat.15
    lt_and_lte(Nat.6, Nat.14, Nat.15)
    Nat.6 < Nat.15
    Nat.6 != Nat.15
    Nat.15 <= Nat.16
    lt_and_lte(Nat.6, Nat.15, Nat.16)
    Nat.6 < Nat.16
    Nat.6 != Nat.16
    Nat.16 <= Nat.17
    lt_and_lte(Nat.6, Nat.16, Nat.17)
    Nat.6 < Nat.17
    Nat.6 != Nat.17
}

/// The label 7 is distinct from every label above it.
theorem eighteen_labels_distinct_row7 {
    Nat.7 != Nat.8 and Nat.7 != Nat.9 and Nat.7 != Nat.10 and Nat.7 != Nat.11 and Nat.7 != Nat.12 and Nat.7 != Nat.13 and Nat.7 != Nat.14 and Nat.7 != Nat.15 and Nat.7 != Nat.16 and Nat.7 != Nat.17
} by {
    eighteen_labels_lt_ladder
    (Nat.0 < Nat.1 and Nat.1 < Nat.2 and Nat.2 < Nat.3 and Nat.3 < Nat.4 and Nat.4 < Nat.5 and Nat.5 < Nat.6 and Nat.6 < Nat.7 and Nat.7 < Nat.8 and Nat.8 < Nat.9 and Nat.9 < Nat.10 and Nat.10 < Nat.11 and Nat.11 < Nat.12 and Nat.12 < Nat.13 and Nat.13 < Nat.14 and Nat.14 < Nat.15 and Nat.15 < Nat.16 and Nat.16 < Nat.17)
    Nat.7 <= Nat.8
    lt_and_lte(Nat.7, Nat.7, Nat.8)
    Nat.7 < Nat.8
    Nat.7 != Nat.8
    Nat.8 <= Nat.9
    lt_and_lte(Nat.7, Nat.8, Nat.9)
    Nat.7 < Nat.9
    Nat.7 != Nat.9
    Nat.9 <= Nat.10
    lt_and_lte(Nat.7, Nat.9, Nat.10)
    Nat.7 < Nat.10
    Nat.7 != Nat.10
    Nat.10 <= Nat.11
    lt_and_lte(Nat.7, Nat.10, Nat.11)
    Nat.7 < Nat.11
    Nat.7 != Nat.11
    Nat.11 <= Nat.12
    lt_and_lte(Nat.7, Nat.11, Nat.12)
    Nat.7 < Nat.12
    Nat.7 != Nat.12
    Nat.12 <= Nat.13
    lt_and_lte(Nat.7, Nat.12, Nat.13)
    Nat.7 < Nat.13
    Nat.7 != Nat.13
    Nat.13 <= Nat.14
    lt_and_lte(Nat.7, Nat.13, Nat.14)
    Nat.7 < Nat.14
    Nat.7 != Nat.14
    Nat.14 <= Nat.15
    lt_and_lte(Nat.7, Nat.14, Nat.15)
    Nat.7 < Nat.15
    Nat.7 != Nat.15
    Nat.15 <= Nat.16
    lt_and_lte(Nat.7, Nat.15, Nat.16)
    Nat.7 < Nat.16
    Nat.7 != Nat.16
    Nat.16 <= Nat.17
    lt_and_lte(Nat.7, Nat.16, Nat.17)
    Nat.7 < Nat.17
    Nat.7 != Nat.17
}

/// The label 8 is distinct from every label above it.
theorem eighteen_labels_distinct_row8 {
    Nat.8 != Nat.9 and Nat.8 != Nat.10 and Nat.8 != Nat.11 and Nat.8 != Nat.12 and Nat.8 != Nat.13 and Nat.8 != Nat.14 and Nat.8 != Nat.15 and Nat.8 != Nat.16 and Nat.8 != Nat.17
} by {
    eighteen_labels_lt_ladder
    (Nat.0 < Nat.1 and Nat.1 < Nat.2 and Nat.2 < Nat.3 and Nat.3 < Nat.4 and Nat.4 < Nat.5 and Nat.5 < Nat.6 and Nat.6 < Nat.7 and Nat.7 < Nat.8 and Nat.8 < Nat.9 and Nat.9 < Nat.10 and Nat.10 < Nat.11 and Nat.11 < Nat.12 and Nat.12 < Nat.13 and Nat.13 < Nat.14 and Nat.14 < Nat.15 and Nat.15 < Nat.16 and Nat.16 < Nat.17)
    Nat.8 <= Nat.9
    lt_and_lte(Nat.8, Nat.8, Nat.9)
    Nat.8 < Nat.9
    Nat.8 != Nat.9
    Nat.9 <= Nat.10
    lt_and_lte(Nat.8, Nat.9, Nat.10)
    Nat.8 < Nat.10
    Nat.8 != Nat.10
    Nat.10 <= Nat.11
    lt_and_lte(Nat.8, Nat.10, Nat.11)
    Nat.8 < Nat.11
    Nat.8 != Nat.11
    Nat.11 <= Nat.12
    lt_and_lte(Nat.8, Nat.11, Nat.12)
    Nat.8 < Nat.12
    Nat.8 != Nat.12
    Nat.12 <= Nat.13
    lt_and_lte(Nat.8, Nat.12, Nat.13)
    Nat.8 < Nat.13
    Nat.8 != Nat.13
    Nat.13 <= Nat.14
    lt_and_lte(Nat.8, Nat.13, Nat.14)
    Nat.8 < Nat.14
    Nat.8 != Nat.14
    Nat.14 <= Nat.15
    lt_and_lte(Nat.8, Nat.14, Nat.15)
    Nat.8 < Nat.15
    Nat.8 != Nat.15
    Nat.15 <= Nat.16
    lt_and_lte(Nat.8, Nat.15, Nat.16)
    Nat.8 < Nat.16
    Nat.8 != Nat.16
    Nat.16 <= Nat.17
    lt_and_lte(Nat.8, Nat.16, Nat.17)
    Nat.8 < Nat.17
    Nat.8 != Nat.17
}

/// The label 9 is distinct from every label above it.
theorem eighteen_labels_distinct_row9 {
    Nat.9 != Nat.10 and Nat.9 != Nat.11 and Nat.9 != Nat.12 and Nat.9 != Nat.13 and Nat.9 != Nat.14 and Nat.9 != Nat.15 and Nat.9 != Nat.16 and Nat.9 != Nat.17
} by {
    eighteen_labels_lt_ladder
    (Nat.0 < Nat.1 and Nat.1 < Nat.2 and Nat.2 < Nat.3 and Nat.3 < Nat.4 and Nat.4 < Nat.5 and Nat.5 < Nat.6 and Nat.6 < Nat.7 and Nat.7 < Nat.8 and Nat.8 < Nat.9 and Nat.9 < Nat.10 and Nat.10 < Nat.11 and Nat.11 < Nat.12 and Nat.12 < Nat.13 and Nat.13 < Nat.14 and Nat.14 < Nat.15 and Nat.15 < Nat.16 and Nat.16 < Nat.17)
    Nat.9 <= Nat.10
    lt_and_lte(Nat.9, Nat.9, Nat.10)
    Nat.9 < Nat.10
    Nat.9 != Nat.10
    Nat.10 <= Nat.11
    lt_and_lte(Nat.9, Nat.10, Nat.11)
    Nat.9 < Nat.11
    Nat.9 != Nat.11
    Nat.11 <= Nat.12
    lt_and_lte(Nat.9, Nat.11, Nat.12)
    Nat.9 < Nat.12
    Nat.9 != Nat.12
    Nat.12 <= Nat.13
    lt_and_lte(Nat.9, Nat.12, Nat.13)
    Nat.9 < Nat.13
    Nat.9 != Nat.13
    Nat.13 <= Nat.14
    lt_and_lte(Nat.9, Nat.13, Nat.14)
    Nat.9 < Nat.14
    Nat.9 != Nat.14
    Nat.14 <= Nat.15
    lt_and_lte(Nat.9, Nat.14, Nat.15)
    Nat.9 < Nat.15
    Nat.9 != Nat.15
    Nat.15 <= Nat.16
    lt_and_lte(Nat.9, Nat.15, Nat.16)
    Nat.9 < Nat.16
    Nat.9 != Nat.16
    Nat.16 <= Nat.17
    lt_and_lte(Nat.9, Nat.16, Nat.17)
    Nat.9 < Nat.17
    Nat.9 != Nat.17
}

/// The label 10 is distinct from every label above it.
theorem eighteen_labels_distinct_row10 {
    Nat.10 != Nat.11 and Nat.10 != Nat.12 and Nat.10 != Nat.13 and Nat.10 != Nat.14 and Nat.10 != Nat.15 and Nat.10 != Nat.16 and Nat.10 != Nat.17
} by {
    eighteen_labels_lt_ladder
    (Nat.0 < Nat.1 and Nat.1 < Nat.2 and Nat.2 < Nat.3 and Nat.3 < Nat.4 and Nat.4 < Nat.5 and Nat.5 < Nat.6 and Nat.6 < Nat.7 and Nat.7 < Nat.8 and Nat.8 < Nat.9 and Nat.9 < Nat.10 and Nat.10 < Nat.11 and Nat.11 < Nat.12 and Nat.12 < Nat.13 and Nat.13 < Nat.14 and Nat.14 < Nat.15 and Nat.15 < Nat.16 and Nat.16 < Nat.17)
    Nat.10 <= Nat.11
    lt_and_lte(Nat.10, Nat.10, Nat.11)
    Nat.10 < Nat.11
    Nat.10 != Nat.11
    Nat.11 <= Nat.12
    lt_and_lte(Nat.10, Nat.11, Nat.12)
    Nat.10 < Nat.12
    Nat.10 != Nat.12
    Nat.12 <= Nat.13
    lt_and_lte(Nat.10, Nat.12, Nat.13)
    Nat.10 < Nat.13
    Nat.10 != Nat.13
    Nat.13 <= Nat.14
    lt_and_lte(Nat.10, Nat.13, Nat.14)
    Nat.10 < Nat.14
    Nat.10 != Nat.14
    Nat.14 <= Nat.15
    lt_and_lte(Nat.10, Nat.14, Nat.15)
    Nat.10 < Nat.15
    Nat.10 != Nat.15
    Nat.15 <= Nat.16
    lt_and_lte(Nat.10, Nat.15, Nat.16)
    Nat.10 < Nat.16
    Nat.10 != Nat.16
    Nat.16 <= Nat.17
    lt_and_lte(Nat.10, Nat.16, Nat.17)
    Nat.10 < Nat.17
    Nat.10 != Nat.17
}

/// The label 11 is distinct from every label above it.
theorem eighteen_labels_distinct_row11 {
    Nat.11 != Nat.12 and Nat.11 != Nat.13 and Nat.11 != Nat.14 and Nat.11 != Nat.15 and Nat.11 != Nat.16 and Nat.11 != Nat.17
} by {
    eighteen_labels_lt_ladder
    (Nat.0 < Nat.1 and Nat.1 < Nat.2 and Nat.2 < Nat.3 and Nat.3 < Nat.4 and Nat.4 < Nat.5 and Nat.5 < Nat.6 and Nat.6 < Nat.7 and Nat.7 < Nat.8 and Nat.8 < Nat.9 and Nat.9 < Nat.10 and Nat.10 < Nat.11 and Nat.11 < Nat.12 and Nat.12 < Nat.13 and Nat.13 < Nat.14 and Nat.14 < Nat.15 and Nat.15 < Nat.16 and Nat.16 < Nat.17)
    Nat.11 <= Nat.12
    lt_and_lte(Nat.11, Nat.11, Nat.12)
    Nat.11 < Nat.12
    Nat.11 != Nat.12
    Nat.12 <= Nat.13
    lt_and_lte(Nat.11, Nat.12, Nat.13)
    Nat.11 < Nat.13
    Nat.11 != Nat.13
    Nat.13 <= Nat.14
    lt_and_lte(Nat.11, Nat.13, Nat.14)
    Nat.11 < Nat.14
    Nat.11 != Nat.14
    Nat.14 <= Nat.15
    lt_and_lte(Nat.11, Nat.14, Nat.15)
    Nat.11 < Nat.15
    Nat.11 != Nat.15
    Nat.15 <= Nat.16
    lt_and_lte(Nat.11, Nat.15, Nat.16)
    Nat.11 < Nat.16
    Nat.11 != Nat.16
    Nat.16 <= Nat.17
    lt_and_lte(Nat.11, Nat.16, Nat.17)
    Nat.11 < Nat.17
    Nat.11 != Nat.17
}

/// The label 12 is distinct from every label above it.
theorem eighteen_labels_distinct_row12 {
    Nat.12 != Nat.13 and Nat.12 != Nat.14 and Nat.12 != Nat.15 and Nat.12 != Nat.16 and Nat.12 != Nat.17
} by {
    eighteen_labels_lt_ladder
    (Nat.0 < Nat.1 and Nat.1 < Nat.2 and Nat.2 < Nat.3 and Nat.3 < Nat.4 and Nat.4 < Nat.5 and Nat.5 < Nat.6 and Nat.6 < Nat.7 and Nat.7 < Nat.8 and Nat.8 < Nat.9 and Nat.9 < Nat.10 and Nat.10 < Nat.11 and Nat.11 < Nat.12 and Nat.12 < Nat.13 and Nat.13 < Nat.14 and Nat.14 < Nat.15 and Nat.15 < Nat.16 and Nat.16 < Nat.17)
    Nat.12 <= Nat.13
    lt_and_lte(Nat.12, Nat.12, Nat.13)
    Nat.12 < Nat.13
    Nat.12 != Nat.13
    Nat.13 <= Nat.14
    lt_and_lte(Nat.12, Nat.13, Nat.14)
    Nat.12 < Nat.14
    Nat.12 != Nat.14
    Nat.14 <= Nat.15
    lt_and_lte(Nat.12, Nat.14, Nat.15)
    Nat.12 < Nat.15
    Nat.12 != Nat.15
    Nat.15 <= Nat.16
    lt_and_lte(Nat.12, Nat.15, Nat.16)
    Nat.12 < Nat.16
    Nat.12 != Nat.16
    Nat.16 <= Nat.17
    lt_and_lte(Nat.12, Nat.16, Nat.17)
    Nat.12 < Nat.17
    Nat.12 != Nat.17
}

/// The label 13 is distinct from every label above it.
theorem eighteen_labels_distinct_row13 {
    Nat.13 != Nat.14 and Nat.13 != Nat.15 and Nat.13 != Nat.16 and Nat.13 != Nat.17
} by {
    eighteen_labels_lt_ladder
    (Nat.0 < Nat.1 and Nat.1 < Nat.2 and Nat.2 < Nat.3 and Nat.3 < Nat.4 and Nat.4 < Nat.5 and Nat.5 < Nat.6 and Nat.6 < Nat.7 and Nat.7 < Nat.8 and Nat.8 < Nat.9 and Nat.9 < Nat.10 and Nat.10 < Nat.11 and Nat.11 < Nat.12 and Nat.12 < Nat.13 and Nat.13 < Nat.14 and Nat.14 < Nat.15 and Nat.15 < Nat.16 and Nat.16 < Nat.17)
    Nat.13 <= Nat.14
    lt_and_lte(Nat.13, Nat.13, Nat.14)
    Nat.13 < Nat.14
    Nat.13 != Nat.14
    Nat.14 <= Nat.15
    lt_and_lte(Nat.13, Nat.14, Nat.15)
    Nat.13 < Nat.15
    Nat.13 != Nat.15
    Nat.15 <= Nat.16
    lt_and_lte(Nat.13, Nat.15, Nat.16)
    Nat.13 < Nat.16
    Nat.13 != Nat.16
    Nat.16 <= Nat.17
    lt_and_lte(Nat.13, Nat.16, Nat.17)
    Nat.13 < Nat.17
    Nat.13 != Nat.17
}

/// The label 14 is distinct from every label above it.
theorem eighteen_labels_distinct_row14 {
    Nat.14 != Nat.15 and Nat.14 != Nat.16 and Nat.14 != Nat.17
} by {
    eighteen_labels_lt_ladder
    (Nat.0 < Nat.1 and Nat.1 < Nat.2 and Nat.2 < Nat.3 and Nat.3 < Nat.4 and Nat.4 < Nat.5 and Nat.5 < Nat.6 and Nat.6 < Nat.7 and Nat.7 < Nat.8 and Nat.8 < Nat.9 and Nat.9 < Nat.10 and Nat.10 < Nat.11 and Nat.11 < Nat.12 and Nat.12 < Nat.13 and Nat.13 < Nat.14 and Nat.14 < Nat.15 and Nat.15 < Nat.16 and Nat.16 < Nat.17)
    Nat.14 <= Nat.15
    lt_and_lte(Nat.14, Nat.14, Nat.15)
    Nat.14 < Nat.15
    Nat.14 != Nat.15
    Nat.15 <= Nat.16
    lt_and_lte(Nat.14, Nat.15, Nat.16)
    Nat.14 < Nat.16
    Nat.14 != Nat.16
    Nat.16 <= Nat.17
    lt_and_lte(Nat.14, Nat.16, Nat.17)
    Nat.14 < Nat.17
    Nat.14 != Nat.17
}

/// The label 15 is distinct from every label above it.
theorem eighteen_labels_distinct_row15 {
    Nat.15 != Nat.16 and Nat.15 != Nat.17
} by {
    eighteen_labels_lt_ladder
    (Nat.0 < Nat.1 and Nat.1 < Nat.2 and Nat.2 < Nat.3 and Nat.3 < Nat.4 and Nat.4 < Nat.5 and Nat.5 < Nat.6 and Nat.6 < Nat.7 and Nat.7 < Nat.8 and Nat.8 < Nat.9 and Nat.9 < Nat.10 and Nat.10 < Nat.11 and Nat.11 < Nat.12 and Nat.12 < Nat.13 and Nat.13 < Nat.14 and Nat.14 < Nat.15 and Nat.15 < Nat.16 and Nat.16 < Nat.17)
    Nat.15 <= Nat.16
    lt_and_lte(Nat.15, Nat.15, Nat.16)
    Nat.15 < Nat.16
    Nat.15 != Nat.16
    Nat.16 <= Nat.17
    lt_and_lte(Nat.15, Nat.16, Nat.17)
    Nat.15 < Nat.17
    Nat.15 != Nat.17
}

/// The label 16 is distinct from every label above it.
theorem eighteen_labels_distinct_row16 {
    Nat.16 != Nat.17
} by {
    eighteen_labels_lt_ladder
    (Nat.0 < Nat.1 and Nat.1 < Nat.2 and Nat.2 < Nat.3 and Nat.3 < Nat.4 and Nat.4 < Nat.5 and Nat.5 < Nat.6 and Nat.6 < Nat.7 and Nat.7 < Nat.8 and Nat.8 < Nat.9 and Nat.9 < Nat.10 and Nat.10 < Nat.11 and Nat.11 < Nat.12 and Nat.12 < Nat.13 and Nat.13 < Nat.14 and Nat.14 < Nat.15 and Nat.15 < Nat.16 and Nat.16 < Nat.17)
    Nat.16 <= Nat.17
    lt_and_lte(Nat.16, Nat.16, Nat.17)
    Nat.16 < Nat.17
    Nat.16 != Nat.17
}
